---
phase: 17-integration-polish
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - CHANGELOG.md
  - package.json
  - CLAUDE.md
autonomous: true
requirements:
  - INT-05
  - INT-06
must_haves:
  truths:
    - "CLAUDE.md documents the memory and adaptive skill systems"
    - "CHANGELOG has release notes covering all Phase 12-17 features"
    - "package.json version is bumped to 1.6.0"
    - "CI passes (lint + type-check + tests)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release notes for v1.6.0"
      contains: "1.6.0"
    - path: "package.json"
      provides: "Version bump"
      contains: "1.6.0"
    - path: "CLAUDE.md"
      provides: "Updated project documentation"
      contains: "memory"
  key_links:
    - from: "CLAUDE.md"
      to: "src/memory/"
      via: "documentation reference"
      pattern: "memory"
    - from: "CHANGELOG.md"
      to: "package.json"
      via: "version consistency"
      pattern: "1.6.0"
---

<objective>
Full polish pass: update documentation (CLAUDE.md, CHANGELOG.md), bump version, and ensure CI readiness.

Purpose: Per D-09/D-10/D-11/D-12, make every feature feel like it was designed as a cohesive system. Update CLAUDE.md to document the memory system, adaptive skills, and observability. Write comprehensive CHANGELOG entries. Bump version to 1.6.0 (minor, per D-11 — not a major version).

Output: Updated docs, version bump, ready for release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-integration-polish/17-CONTEXT.md
@.planning/phases/17-integration-polish/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with memory, skills, and observability documentation</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md
    - src/memory/retrieval.ts
    - src/skills/adaptive-injector.ts
    - src/observability/event-store.ts
    - src/index.ts
  </read_first>
  <action>
Per D-09/D-10: Update CLAUDE.md to document all v3.0 features so new developers and AI assistants understand the full system.

Add these sections to the Architecture description in CLAUDE.md:

1. **Memory System** section under Architecture:
   ```
   src/memory/               Smart dual-scope memory (project patterns + user preferences)
     database.ts             bun:sqlite with FTS5, schema initialization
     capture.ts              Event-driven observation capture from session events
     retrieval.ts            3-layer progressive disclosure with token-budgeted context
     injector.ts             System prompt injection via experimental.chat.system.transform
     decay.ts                Relevance scoring with configurable half-life decay
     repository.ts           SQLite CRUD for observations, projects, preferences
   ```

2. **Observability** section under Architecture:
   ```
   src/observability/        Session observability and structured event logging
     event-store.ts          In-memory session event collection
     event-handlers.ts       Hook handlers for event/tool lifecycle
     context-monitor.ts      Token tracking and context usage monitoring
     log-writer.ts           JSON log persistence to ~/.config/opencode/logs/
     retention.ts            Time-based log pruning (30-day default)
   ```

3. **Skills Infrastructure** section under Architecture:
   ```
   src/skills/               Adaptive skill loading and injection
     loader.ts               Load all skill directories from assets
     adaptive-injector.ts    Stack detection, filtering, dependency ordering, token budgeting
     dependency-resolver.ts  Topological sort for skill execution order
     linter.ts               Asset validation for skill/agent/command files
   ```

4. Update the **Dependency flow** line to include: `index.ts` -> `tools/*` -> `templates/*` + `utils/*` + `memory/*` + `skills/*` + `observability/*`

5. Update the **Commands** section to reflect actual test count (check with `bun test` output).

6. Add to **Key Patterns**:
   - **Best-effort injection:** Memory and skill context injection never blocks or crashes the pipeline. All injection functions swallow errors and return empty strings on failure.
   - **Token budgeting:** Both memory and skill injection enforce character-per-token limits to prevent context overflow.

Do NOT add any AI attribution. Keep the tone factual and concise, matching the existing CLAUDE.md style.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-autopilot && grep -q "memory" CLAUDE.md && grep -q "observability" CLAUDE.md && grep -q "adaptive" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "src/memory/" CLAUDE.md returns 0
    - grep -q "src/observability/" CLAUDE.md returns 0
    - grep -q "src/skills/" CLAUDE.md returns 0
    - grep -q "Best-effort injection" CLAUDE.md returns 0
    - grep -qv "Claude\|AI-generated\|Co-Authored" CLAUDE.md (no AI attribution)
  </acceptance_criteria>
  <done>CLAUDE.md documents all v3.0 subsystems (memory, observability, skills) with architecture descriptions and key patterns</done>
</task>

<task type="auto">
  <name>Task 2: Version bump and CHANGELOG update</name>
  <files>package.json, CHANGELOG.md</files>
  <read_first>
    - package.json
    - CHANGELOG.md
  </read_first>
  <action>
Per D-11/D-12: Normal minor release (not v3.0 major version).

1. In `package.json`: Change `"version": "1.5.0"` to `"version": "1.6.0"`

2. In `CHANGELOG.md`: Add a new section at the top (before the existing 1.5.0 entry):

```markdown
## [1.6.0] (2026-04-02)

### Features

* **17-01:** wire adaptive skill routing into orchestrator dispatch (replaces single coding-standards injection)
* **17-01:** add memory-based confidence tuning for Arena debate depth
* **17-02:** add cross-feature integration tests (orchestrator + skills + memory)
* **17-02:** add config migration chain v1-v5 integration test
* **17-03:** update CLAUDE.md with memory, observability, and skills documentation

### Phase 15 (Memory System)

* Smart dual-scope memory with project patterns and user preferences
* bun:sqlite with FTS5 for full-text search
* 3-layer progressive disclosure retrieval with token budgeting
* System prompt injection via experimental.chat.system.transform hook
* Relevance scoring with configurable 90-day half-life decay

### Phase 13 (Session Observability)

* Structured event logging with JSON persistence
* Token tracking and context monitoring
* Session summaries and pipeline reports
* Time-based log retention (30-day default)
* Mock provider for fallback chain testing
```

The CHANGELOG should be factual, listing concrete features. No marketing language.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-autopilot && grep -q '"version": "1.6.0"' package.json && grep -q "1.6.0" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"version": "1.6.0"' package.json returns 0
    - grep -q "1.6.0" CHANGELOG.md returns 0
    - grep -q "adaptive skill routing" CHANGELOG.md returns 0
    - grep -q "memory-based confidence" CHANGELOG.md returns 0
  </acceptance_criteria>
  <done>package.json version bumped to 1.6.0; CHANGELOG has comprehensive release notes for all Phase 12-17 features</done>
</task>

</tasks>

<verification>
- `grep -q "1.6.0" package.json` confirms version bump
- `grep -q "src/memory/" CLAUDE.md` confirms documentation
- `bun run lint` passes
- `bun test` passes (full suite)
</verification>

<success_criteria>
1. CLAUDE.md is a complete reference for the entire codebase including v3.0 features
2. CHANGELOG documents all features shipped since v1.5.0
3. package.json reflects v1.6.0
4. Everything feels cohesive and production-ready (per D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-polish/17-03-SUMMARY.md`
</output>
