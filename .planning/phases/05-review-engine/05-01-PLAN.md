---
phase: 05-review-engine
plan: 01
type: execute
wave: 1
depends_on: [04-01, 04-02]
files_modified:
  - src/review/schemas.ts
  - src/review/types.ts
  - src/review/severity.ts
  - src/review/agent-catalog.ts
  - src/review/stack-gate.ts
  - src/review/team-selection.ts
  - src/review/finding-builder.ts
  - tests/review/schemas.test.ts
  - tests/review/severity.test.ts
  - tests/review/agent-catalog.test.ts
  - tests/review/stack-gate.test.ts
  - tests/review/team-selection.test.ts
  - tests/review/finding-builder.test.ts
autonomous: true
requirements:
  - REVW-02
  - REVW-03
  - REVW-04
  - REVW-05
  - REVW-10

must_haves:
  truths:
    - "Review finding schema validates severity (CRITICAL/WARNING/NITPICK), agent, file, and problem fields"
    - "Agent catalog contains 6+ universal agents and language-specific gated agents"
    - "Stack gate filters agents based on detected project tags, excluding irrelevant specialists"
    - "Team selection scores agents against project stack and returns selected agents with prompts"
    - "Finding builder creates immutable finding objects with proper severity validation"
    - "Severity definitions provide machine-readable criteria for CRITICAL/WARNING/NITPICK"
  artifacts:
    - path: "src/review/schemas.ts"
      provides: "Zod schemas for ReviewFinding, ReviewReport, AgentResult, ReviewConfig"
      exports: ["reviewFindingSchema", "reviewReportSchema", "agentResultSchema", "reviewConfigSchema", "SEVERITIES", "VERDICTS"]
    - path: "src/review/types.ts"
      provides: "TypeScript types inferred from review Zod schemas"
      exports: ["ReviewFinding", "ReviewReport", "AgentResult", "ReviewConfig", "Severity", "Verdict", "AgentCategory", "AgentDefinition"]
    - path: "src/review/severity.ts"
      provides: "Severity definitions with criteria and action requirements"
      exports: ["SEVERITY_DEFINITIONS", "compareSeverity", "isBlockingSeverity"]
    - path: "src/review/agent-catalog.ts"
      provides: "Registry of all review agents with metadata, domain, and stack affinity"
      exports: ["AGENT_CATALOG", "CORE_SQUAD", "getAgentsByCategory"]
    - path: "src/review/stack-gate.ts"
      provides: "Stack-to-agent filtering based on project technology tags"
      exports: ["STACK_GATE_RULES", "applyStackGate", "detectStackTags"]
    - path: "src/review/team-selection.ts"
      provides: "Agent selection logic: score agents against project context, return selected team"
      exports: ["selectReviewTeam", "scoreAgent"]
    - path: "src/review/finding-builder.ts"
      provides: "Immutable finding builder with severity validation"
      exports: ["createFinding", "createAgentResult", "mergeFindings"]
---

<objective>
Create the review engine data layer: Zod schemas, type definitions, severity system, agent catalog, stack gate filtering, team selection logic, and finding builder utilities.

Purpose: Every other Phase 5 module (pipeline, cross-verification, fix cycle, verdict) depends on these types, the agent registry, and the team selection logic. Building them first with full test coverage establishes the contract that the review pipeline will consume.

Output: 7 production files in `src/review/` + 6 test files in `tests/review/`
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation-infrastructure/04-CONTEXT.md

@src/orchestrator/schemas.ts (reference Zod pattern)
@src/orchestrator/types.ts (reference type inference pattern)
@src/config.ts (reference Zod validation pattern)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Review Zod schemas and types</name>
  <files>src/review/schemas.ts, src/review/types.ts, tests/review/schemas.test.ts</files>
  <behavior>
    - Test: reviewFindingSchema.parse(validFinding) returns typed ReviewFinding
    - Test: reviewFindingSchema.parse with invalid severity throws ZodError
    - Test: agentResultSchema.parse with findings array succeeds
    - Test: reviewReportSchema validates verdict as CLEAN|APPROVED|CONCERNS|BLOCKED
    - Test: reviewConfigSchema has sensible defaults (parallel: true, maxFixAttempts: 3)
    - Test: SEVERITIES contains CRITICAL, WARNING, NITPICK
    - Test: VERDICTS contains CLEAN, APPROVED, CONCERNS, BLOCKED
  </behavior>
  <action>
    Create Zod schemas for the review engine data model following the existing orchestrator/schemas.ts pattern.
  </action>
  <verify>
    <automated>bun test tests/review/schemas.test.ts</automated>
  </verify>
  <done>All review schemas parse valid data and reject invalid data. Types exported. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Severity definitions and comparison</name>
  <files>src/review/severity.ts, tests/review/severity.test.ts</files>
  <behavior>
    - Test: SEVERITY_DEFINITIONS has entries for CRITICAL, WARNING, NITPICK
    - Test: Each severity has criteria array and action string
    - Test: compareSeverity("CRITICAL", "WARNING") returns negative (CRITICAL is higher)
    - Test: compareSeverity("NITPICK", "CRITICAL") returns positive (NITPICK is lower)
    - Test: compareSeverity("WARNING", "WARNING") returns 0
    - Test: isBlockingSeverity("CRITICAL") returns true
    - Test: isBlockingSeverity("WARNING") returns false
    - Test: isBlockingSeverity("NITPICK") returns false
  </behavior>
  <action>
    Create severity definitions module with machine-readable criteria from ace reference.
  </action>
  <verify>
    <automated>bun test tests/review/severity.test.ts</automated>
  </verify>
  <done>Severity definitions provide criteria, comparison, and blocking check. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Agent catalog registry</name>
  <files>src/review/agent-catalog.ts, tests/review/agent-catalog.test.ts</files>
  <behavior>
    - Test: AGENT_CATALOG has 15+ entries
    - Test: CORE_SQUAD contains logic-auditor, test-interrogator, contract-verifier
    - Test: Each agent has name, category, domain, catches, stackAffinity fields
    - Test: getAgentsByCategory("parallel") returns 10+ agents
    - Test: getAgentsByCategory("sequenced") returns product-thinker and red-team
    - Test: getAgentsByCategory("core") returns exactly 3 agents
    - Test: AGENT_CATALOG is frozen (immutable)
  </behavior>
  <action>
    Create agent catalog with all review agents from the ace reference catalog.
  </action>
  <verify>
    <automated>bun test tests/review/agent-catalog.test.ts</automated>
  </verify>
  <done>Agent catalog contains all review agents with metadata. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Stack gate filtering</name>
  <files>src/review/stack-gate.ts, tests/review/stack-gate.test.ts</files>
  <behavior>
    - Test: STACK_GATE_RULES maps agent names to required tags
    - Test: applyStackGate with tags=["typescript"] keeps type-soundness, excludes go-idioms-auditor
    - Test: applyStackGate with tags=["react"] keeps react-patterns-auditor and state-mgmt-auditor
    - Test: applyStackGate with tags=["python"] excludes react-patterns-auditor
    - Test: applyStackGate keeps ungated agents (logic-auditor, security-auditor) regardless of tags
    - Test: applyStackGate with empty tags still keeps ungated agents
    - Test: detectStackTags from file extensions returns correct tags
  </behavior>
  <action>
    Create stack gate module that filters agents based on project technology tags.
  </action>
  <verify>
    <automated>bun test tests/review/stack-gate.test.ts</automated>
  </verify>
  <done>Stack gate correctly filters agents by technology. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Team selection logic</name>
  <files>src/review/team-selection.ts, tests/review/team-selection.test.ts</files>
  <behavior>
    - Test: selectReviewTeam always includes core squad (3 agents)
    - Test: selectReviewTeam for TypeScript project includes type-soundness
    - Test: selectReviewTeam for React project includes react-patterns-auditor
    - Test: selectReviewTeam excludes gated agents for non-matching stacks
    - Test: selectReviewTeam returns agents grouped by category (core, parallel, sequenced)
    - Test: scoreAgent returns 0-10 score for relevance
  </behavior>
  <action>
    Create team selection module that combines agent catalog + stack gate to select review team.
  </action>
  <verify>
    <automated>bun test tests/review/team-selection.test.ts</automated>
  </verify>
  <done>Team selection correctly selects agents based on project stack. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Finding builder utilities</name>
  <files>src/review/finding-builder.ts, tests/review/finding-builder.test.ts</files>
  <behavior>
    - Test: createFinding returns frozen ReviewFinding object
    - Test: createFinding validates severity (rejects invalid values)
    - Test: createAgentResult creates result with agent name, findings array, and duration
    - Test: mergeFindings combines findings from multiple agents, sorted by severity
    - Test: mergeFindings deduplicates findings with same file+title
  </behavior>
  <action>
    Create finding builder utilities for creating and merging review findings.
  </action>
  <verify>
    <automated>bun test tests/review/finding-builder.test.ts</automated>
  </verify>
  <done>Finding builder creates immutable findings with proper validation. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
bun test tests/review/ && bun run lint
```
All 6 test files pass. Lint passes on all new files. No existing tests broken.
</verification>

<success_criteria>
- src/review/ directory exists with schemas.ts, types.ts, severity.ts, agent-catalog.ts, stack-gate.ts, team-selection.ts, finding-builder.ts
- All review Zod schemas reject invalid data and accept valid data
- Agent catalog contains 15+ review agents from ace reference
- Stack gate correctly filters agents by project technology tags
- Team selection combines catalog + stack gate for intelligent agent selection
- Finding builder creates immutable findings with severity validation
- `bun test` passes (all existing + new tests)
- `bun run lint` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-engine/05-01-SUMMARY.md`
</output>
