---
phase: 25-content-agent-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/skills/security-patterns/SKILL.md
  - assets/skills/api-design/SKILL.md
  - assets/skills/database-patterns/SKILL.md
  - assets/skills/docker-deployment/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Four new skill directories exist under assets/skills/ with valid SKILL.md files"
    - "Each skill has YAML frontmatter with name, description, stacks: [], requires: []"
    - "Each skill is 200-400 lines of actionable, language-agnostic patterns"
    - "Skills follow the same format as existing skills (coding-standards, frontend-design)"
  artifacts:
    - path: "assets/skills/security-patterns/SKILL.md"
      provides: "OWASP Top 10, auth patterns, input validation, secret management"
      contains: "name: security-patterns"
    - path: "assets/skills/api-design/SKILL.md"
      provides: "REST/GraphQL conventions, status codes, pagination, versioning"
      contains: "name: api-design"
    - path: "assets/skills/database-patterns/SKILL.md"
      provides: "Query optimization, migration strategies, schema design, indexing"
      contains: "name: database-patterns"
    - path: "assets/skills/docker-deployment/SKILL.md"
      provides: "Dockerfile best practices, multi-stage builds, docker-compose, CI/CD"
      contains: "name: docker-deployment"
  key_links:
    - from: "assets/skills/*/SKILL.md"
      to: "src/skills/loader.ts"
      via: "Auto-discovery by loader.ts readdir"
      pattern: "readdir"
---

<objective>
Create 4 new universal skills: security-patterns, api-design, database-patterns, and docker-deployment.

Purpose: Close skill coverage gaps identified in Phase 11 gap analysis. These 4 skills bring the total from 18 to 22, covering the most requested cross-cutting development domains. All are universal (stacks: []) since security, API design, database, and Docker patterns are language-agnostic.

Output: 4 new SKILL.md files in assets/skills/, auto-discovered by the skill loader on next plugin load.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/skills/coding-standards/SKILL.md
@assets/skills/frontend-design/SKILL.md
</context>

<interfaces>
<!-- Skill YAML frontmatter format (from existing skills): -->
```yaml
---
name: skill-name
description: One-line description
stacks:
  - stack1  # or empty [] for universal
requires: []
---
```

<!-- Skill loader auto-discovers directories under assets/skills/ — no registration needed -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create security-patterns and api-design skills</name>
  <files>assets/skills/security-patterns/SKILL.md, assets/skills/api-design/SKILL.md</files>
  <read_first>
    assets/skills/coding-standards/SKILL.md
    assets/skills/frontend-design/SKILL.md
  </read_first>
  <action>
Create two new skill directories and SKILL.md files following the exact format of existing skills (YAML frontmatter + markdown body with numbered sections).

**security-patterns/SKILL.md** (per D-07):
- YAML: `name: security-patterns`, `description: OWASP Top 10 security patterns...`, `stacks: []`, `requires: []`
- Sections covering: OWASP Top 10 (injection, broken auth, XSS, CSRF, SSRF, etc.), authentication patterns (JWT, session, OAuth2), authorization (RBAC, ABAC), input validation and sanitization, secret management (env vars, vault, rotation), secure headers (CSP, HSTS, X-Frame-Options), dependency security (audit, lockfiles, SCA), cryptography basics (hashing, encryption, signing), API security (rate limiting, CORS, API keys), logging and monitoring (audit trails, PII redaction)
- Each section: DO/DON'T format with concrete examples, not abstract theory
- Target 250-350 lines

**api-design/SKILL.md** (per D-08):
- YAML: `name: api-design`, `description: REST and GraphQL API design conventions...`, `stacks: []`, `requires: []`
- Sections covering: REST resource naming (plural nouns, nesting limits), HTTP method semantics (GET/POST/PUT/PATCH/DELETE), status code selection guide (2xx, 3xx, 4xx, 5xx with when-to-use), pagination patterns (cursor vs offset, Link headers), filtering and sorting (query param conventions), versioning strategies (URL path vs header vs content negotiation), error response format (type, title, status, detail, instance per RFC 7807), GraphQL schema design (queries vs mutations, input types, pagination with connections), request/response examples, rate limiting headers (X-RateLimit-*), HATEOAS basics, idempotency keys
- Each section: concrete examples with request/response snippets
- Target 250-350 lines
  </action>
  <verify>
    <automated>test -f assets/skills/security-patterns/SKILL.md && test -f assets/skills/api-design/SKILL.md && head -5 assets/skills/security-patterns/SKILL.md | grep -q "name: security-patterns" && head -5 assets/skills/api-design/SKILL.md | grep -q "name: api-design" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `assets/skills/security-patterns/SKILL.md` exists with `name: security-patterns` in frontmatter
    - File `assets/skills/api-design/SKILL.md` exists with `name: api-design` in frontmatter
    - Both have `stacks: []` (universal)
    - Both have `requires: []`
    - security-patterns contains sections on OWASP, authentication, authorization, input validation, secret management
    - api-design contains sections on REST naming, HTTP methods, status codes, pagination, error format
    - Each file is 200-400 lines
  </acceptance_criteria>
  <done>Two skill files created with valid frontmatter and comprehensive, actionable content in DO/DON'T format</done>
</task>

<task type="auto">
  <name>Task 2: Create database-patterns and docker-deployment skills</name>
  <files>assets/skills/database-patterns/SKILL.md, assets/skills/docker-deployment/SKILL.md</files>
  <read_first>
    assets/skills/coding-standards/SKILL.md
    assets/skills/security-patterns/SKILL.md
  </read_first>
  <action>
Create two more skill directories and SKILL.md files.

**database-patterns/SKILL.md** (per D-09):
- YAML: `name: database-patterns`, `description: Database design, query optimization, and migration patterns...`, `stacks: []`, `requires: []`
- Sections covering: Schema design principles (normalization, denormalization trade-offs, naming conventions), indexing strategy (B-tree vs hash, composite indexes, covering indexes, when NOT to index), query optimization (EXPLAIN analysis, N+1 detection, batch operations, query plan reading), migration strategies (versioned migrations, zero-downtime migrations, rollback planning, data backfills), connection pooling (pool sizing, connection lifecycle, timeout configuration), transactions (isolation levels, deadlock prevention, optimistic vs pessimistic locking), data modeling patterns (soft deletes, temporal data, polymorphic associations, JSON columns), backup and recovery (point-in-time recovery, backup verification, disaster planning)
- Each section: DO/DON'T with concrete SQL/ORM examples
- Target 250-350 lines

**docker-deployment/SKILL.md** (per D-10):
- YAML: `name: docker-deployment`, `description: Dockerfile best practices, container orchestration, and CI/CD deployment patterns...`, `stacks: []`, `requires: []`
- Sections covering: Dockerfile best practices (multi-stage builds, layer caching, minimal base images, non-root users, .dockerignore), docker-compose patterns (service dependencies, health checks, volume management, networking), CI/CD pipeline design (build/test/deploy stages, caching strategies, artifact management, environment promotion), container security (image scanning, read-only filesystems, resource limits, secret injection), health checks and readiness probes (HTTP, TCP, exec patterns), logging and monitoring (structured logs, log aggregation, container metrics), deployment strategies (rolling updates, blue-green, canary), environment configuration (12-factor app, config maps, secret management)
- Each section: concrete Dockerfile/YAML/config examples
- Target 250-350 lines
  </action>
  <verify>
    <automated>test -f assets/skills/database-patterns/SKILL.md && test -f assets/skills/docker-deployment/SKILL.md && head -5 assets/skills/database-patterns/SKILL.md | grep -q "name: database-patterns" && head -5 assets/skills/docker-deployment/SKILL.md | grep -q "name: docker-deployment" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `assets/skills/database-patterns/SKILL.md` exists with `name: database-patterns` in frontmatter
    - File `assets/skills/docker-deployment/SKILL.md` exists with `name: docker-deployment` in frontmatter
    - Both have `stacks: []` (universal)
    - Both have `requires: []`
    - database-patterns contains sections on schema design, indexing, query optimization, migrations, connection pooling
    - docker-deployment contains sections on Dockerfile best practices, multi-stage builds, docker-compose, CI/CD, health checks
    - Each file is 200-400 lines
  </acceptance_criteria>
  <done>Two skill files created with valid frontmatter and comprehensive, actionable content with concrete examples</done>
</task>

</tasks>

<verification>
- All 4 skill directories exist under assets/skills/
- Each SKILL.md has valid YAML frontmatter with name, description, stacks: [], requires: []
- `bun test` passes (no regressions)
- `bun run lint` passes
- Total skill count is now 22 (18 existing + 4 new)
</verification>

<success_criteria>
- 4 new universal skills created: security-patterns, api-design, database-patterns, docker-deployment
- Each skill is 200-400 lines of actionable, DO/DON'T formatted patterns
- Skills auto-load via existing loader (no code changes needed)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-content-agent-expansion/25-01-SUMMARY.md`
</output>
