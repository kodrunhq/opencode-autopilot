---
phase: 20-new-primary-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/debugger.ts
  - src/agents/planner.ts
  - src/agents/reviewer.ts
autonomous: true
requirements:
  - AGNT-10
  - AGNT-11
  - AGNT-12

must_haves:
  truths:
    - "Debugger agent source file exists with systematic-debugging skill embedded in prompt"
    - "Planner agent source file exists with plan-writing and plan-executing skills embedded in prompt"
    - "Reviewer agent source file exists with code-review skill embedded in prompt and oc_review auto-invocation instruction"
    - "All three agents use mode 'all' for Tab-cycle and @ autocomplete visibility"
    - "All three agents have role-scoped permissions per CONTEXT decisions"
  artifacts:
    - path: "src/agents/debugger.ts"
      provides: "Primary debugger agent with embedded systematic-debugging skill"
      exports: ["debuggerAgent"]
      contains: "mode: \"all\""
    - path: "src/agents/planner.ts"
      provides: "Primary planner agent with embedded plan-writing and plan-executing skills"
      exports: ["plannerAgent"]
      contains: "mode: \"all\""
    - path: "src/agents/reviewer.ts"
      provides: "Primary code reviewer agent with embedded code-review skill"
      exports: ["reviewerAgent"]
      contains: "mode: \"all\""
  key_links:
    - from: "src/agents/debugger.ts"
      to: "@opencode-ai/sdk"
      via: "AgentConfig type import"
      pattern: "import type.*AgentConfig.*@opencode-ai/sdk"
    - from: "src/agents/planner.ts"
      to: "@opencode-ai/sdk"
      via: "AgentConfig type import"
      pattern: "import type.*AgentConfig.*@opencode-ai/sdk"
    - from: "src/agents/reviewer.ts"
      to: "@opencode-ai/sdk"
      via: "AgentConfig type import"
      pattern: "import type.*AgentConfig.*@opencode-ai/sdk"
---

<objective>
Create the three new primary agent source files -- Debugger, Planner, and Code Reviewer -- each with skill content statically embedded in the prompt, role-scoped permissions, and mode "all" for Tab-cycle visibility.

Purpose: These agents give users direct access to specialized capabilities (debugging, planning, code review) via the Tab cycle, without needing to know about the orchestration pipeline.
Output: Three new TypeScript files in src/agents/ following the autopilot.ts pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-new-primary-agents/20-CONTEXT.md
@.planning/phases/20-new-primary-agents/20-RESEARCH.md

<interfaces>
<!-- Reference pattern from src/agents/autopilot.ts -->
From src/agents/autopilot.ts:
```typescript
import type { AgentConfig } from "@opencode-ai/sdk";

export const autopilotAgent: Readonly<AgentConfig> = Object.freeze({
    description: "...",
    mode: "all",
    maxSteps: 50,
    prompt: `...`,
    permission: {
        edit: "allow",
        bash: "allow",
        webfetch: "allow",
    } as const,
});
```

SDK AgentConfig permission type fields: edit, bash, webfetch, doom_loop, external_directory
- Values: "allow" | "deny" | "ask"
- Omitted fields default to "allow" in OpenCode runtime
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Debugger, Planner, and Reviewer agent source files</name>
  <files>src/agents/debugger.ts, src/agents/planner.ts, src/agents/reviewer.ts</files>
  <read_first>
    - src/agents/autopilot.ts (reference pattern for primary agent structure, Object.freeze, permission shape)
    - assets/skills/systematic-debugging/SKILL.md (content to embed in debugger prompt, strip YAML frontmatter)
    - assets/skills/plan-writing/SKILL.md (content to embed in planner prompt, strip YAML frontmatter)
    - assets/skills/plan-executing/SKILL.md (content to embed in planner prompt, strip YAML frontmatter)
    - assets/skills/code-review/SKILL.md (content to embed in reviewer prompt, strip YAML frontmatter)
  </read_first>
  <action>
Create three files following the exact pattern of `autopilot.ts`:

**src/agents/debugger.ts** (per D-01, D-05, D-07, D-11):
- Export: `export const debuggerAgent: Readonly<AgentConfig> = Object.freeze({...})`
- Import: `import type { AgentConfig } from "@opencode-ai/sdk"`
- `description`: "Systematic bug diagnosis: Reproduce, Isolate, Diagnose, Fix -- with regression tests"
- `mode`: "all"
- `maxSteps`: 25
- `prompt`: A template literal containing:
  - Opening identity and role paragraph ("You are the debugger agent...")
  - A "How You Work" section explaining the 4-phase process
  - A `<skill name="systematic-debugging">` section with the FULL content of `assets/skills/systematic-debugging/SKILL.md` minus the YAML frontmatter (lines 1-6, the `---` delimited block). Include everything from "# Systematic Debugging" onward.
  - A "Rules" section with:
    - ALWAYS follow the 4-phase process in order. Do not skip to Fix.
    - ALWAYS write a regression test before applying the fix.
    - Use bash to run tests, git bisect, and reproduce bugs.
    - Use edit to apply fixes after diagnosis.
    - NEVER make random changes hoping something works (shotgun debugging).
    - NEVER fix symptoms without understanding the root cause.
- `permission`: `{ edit: "allow", bash: "allow", webfetch: "deny" } as const`

**src/agents/planner.ts** (per D-01, D-05, D-08, D-11):
- Export: `export const plannerAgent: Readonly<AgentConfig> = Object.freeze({...})`
- Import: `import type { AgentConfig } from "@opencode-ai/sdk"`
- `description`: "Decompose features into bite-sized implementation plans with file paths, dependencies, and verification criteria"
- `mode`: "all"
- `maxSteps`: 20
- `prompt`: A template literal containing:
  - Opening identity and role paragraph ("You are the planner agent...")
  - A "How You Work" section explaining the planning methodology
  - A `<skill name="plan-writing">` section with the FULL content of `assets/skills/plan-writing/SKILL.md` minus YAML frontmatter (lines 1-5). Include everything from "# Plan Writing" onward.
  - A `<skill name="plan-executing">` section with the FULL content of `assets/skills/plan-executing/SKILL.md` minus YAML frontmatter (lines 1-6). Include everything from "# Plan Executing" onward.
  - A "Rules" section with:
    - ALWAYS write the plan before writing any code.
    - ALWAYS include exact file paths in every task.
    - ALWAYS include verification commands for every task.
    - Write plans as markdown files in the current working directory.
    - NEVER implement code directly -- your job is to plan, not build.
    - NEVER skip dependency mapping -- every task must list what it needs and what it creates.
- `permission`: `{ edit: "allow", bash: "allow", webfetch: "deny" } as const`
  - Note: per RESEARCH, the SDK `edit` permission covers both editing existing files and writing new files. D-08 wants "write: allow" (create plan files) and "no edit" (no source code editing). Since the SDK cannot distinguish write-new from edit-existing, we set `edit: "allow"` to enable plan file creation, and enforce the "don't edit source" constraint via the prompt rules above ("NEVER implement code directly").

**src/agents/reviewer.ts** (per D-01, D-05, D-06, D-09, D-10, D-11):
- Export: `export const reviewerAgent: Readonly<AgentConfig> = Object.freeze({...})`
- Import: `import type { AgentConfig } from "@opencode-ai/sdk"`
- `description`: "Multi-agent code review: dispatches specialist reviewers, cross-verifies findings, and reports actionable feedback"
- `mode`: "all"
- `maxSteps`: 30
- `prompt`: A template literal containing:
  - Opening identity and role paragraph ("You are the code reviewer agent...")
  - A "How You Work" section explaining: when asked to review, auto-invoke `oc_review` tool with appropriate scope, then present findings to user with severity levels (per D-10)
  - A `<skill name="code-review">` section with the FULL content of `assets/skills/code-review/SKILL.md` minus YAML frontmatter (lines 1-5). Include everything from "# Code Review" onward.
  - A "Rules" section with:
    - ALWAYS invoke oc_review when the user asks for a review. Do not perform manual review without the tool.
    - Present findings organized by severity: CRITICAL first, then HIGH, MEDIUM, LOW.
    - For each finding, include the file path, line range, issue description, and suggested fix.
    - NEVER apply fixes yourself -- you review, you do not fix. Tell the user what to fix.
    - NEVER approve code without running oc_review first.
    - You are distinct from the pr-reviewer agent. You review code changes (staged, unstaged, or between branches). The pr-reviewer handles GitHub PR-specific workflows.
- `permission`: `{ edit: "deny", bash: "allow", webfetch: "deny" } as const`
  </action>
  <verify>
    <automated>bunx tsc --noEmit src/agents/debugger.ts src/agents/planner.ts src/agents/reviewer.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/agents/debugger.ts contains `export const debuggerAgent: Readonly<AgentConfig>`
    - src/agents/debugger.ts contains `mode: "all"`
    - src/agents/debugger.ts contains `maxSteps: 25`
    - src/agents/debugger.ts contains `webfetch: "deny"`
    - src/agents/debugger.ts contains `# Systematic Debugging` (embedded skill content)
    - src/agents/debugger.ts contains `Object.freeze(`
    - src/agents/planner.ts contains `export const plannerAgent: Readonly<AgentConfig>`
    - src/agents/planner.ts contains `mode: "all"`
    - src/agents/planner.ts contains `maxSteps: 20`
    - src/agents/planner.ts contains `# Plan Writing` (embedded skill)
    - src/agents/planner.ts contains `# Plan Executing` (embedded skill)
    - src/agents/planner.ts contains `Object.freeze(`
    - src/agents/reviewer.ts contains `export const reviewerAgent: Readonly<AgentConfig>`
    - src/agents/reviewer.ts contains `mode: "all"`
    - src/agents/reviewer.ts contains `maxSteps: 30`
    - src/agents/reviewer.ts contains `edit: "deny"`
    - src/agents/reviewer.ts contains `oc_review` (auto-invocation instruction)
    - src/agents/reviewer.ts contains `# Code Review` (embedded skill)
    - src/agents/reviewer.ts contains `Object.freeze(`
    - None of the three files contain `---\nname:` (YAML frontmatter must be stripped)
    - `bunx tsc --noEmit` passes with zero type errors
  </acceptance_criteria>
  <done>Three agent source files exist, each with correct type, mode "all", embedded skill content (YAML frontmatter stripped), role-scoped permissions, and zero TypeScript errors.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` passes with zero errors
- Each file exports a frozen AgentConfig with mode "all"
- Skill content is present in prompts (not just referenced)
- Permissions match CONTEXT decisions mapped through SDK types
</verification>

<success_criteria>
- Three new agent files exist at src/agents/debugger.ts, src/agents/planner.ts, src/agents/reviewer.ts
- All three compile without TypeScript errors
- All three follow the autopilot.ts pattern (Readonly<AgentConfig>, Object.freeze, template literal prompt)
- Skill content is embedded statically (no runtime file reads)
</success_criteria>

<output>
After completion, create `.planning/phases/20-new-primary-agents/20-01-SUMMARY.md`
</output>
