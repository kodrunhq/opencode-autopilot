---
phase: 18-namespace-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/commands/oc-brainstorm.md
  - assets/commands/oc-new-agent.md
  - assets/commands/oc-new-command.md
  - assets/commands/oc-new-skill.md
  - assets/commands/oc-quick.md
  - assets/commands/oc-review-pr.md
  - assets/commands/oc-stocktake.md
  - assets/commands/oc-tdd.md
  - assets/commands/oc-update-docs.md
  - assets/commands/oc-write-plan.md
  - assets/commands/brainstorm.md
  - assets/commands/new-agent.md
  - assets/commands/new-command.md
  - assets/commands/new-skill.md
  - assets/commands/quick.md
  - assets/commands/review-pr.md
  - assets/commands/stocktake.md
  - assets/commands/tdd.md
  - assets/commands/update-docs.md
  - assets/commands/write-plan.md
  - assets/commands/oc-configure.md
  - src/installer.ts
autonomous: true
requirements:
  - BFIX-02
  - BFIX-03

must_haves:
  truths:
    - "Every plugin command is accessible via its oc- prefixed filename"
    - "Old unprefixed command files no longer exist in assets/commands/"
    - "oc-configure.md is deleted from assets/commands/"
    - "Old unprefixed names are listed in DEPRECATED_ASSETS for cleanup on user machines"
    - "FORCE_UPDATE_ASSETS no longer references oc-configure.md"
  artifacts:
    - path: "assets/commands/oc-brainstorm.md"
      provides: "Renamed brainstorm command"
    - path: "assets/commands/oc-tdd.md"
      provides: "Renamed tdd command"
    - path: "assets/commands/oc-quick.md"
      provides: "Renamed quick command"
    - path: "assets/commands/oc-write-plan.md"
      provides: "Renamed write-plan command"
    - path: "assets/commands/oc-stocktake.md"
      provides: "Renamed stocktake command"
    - path: "assets/commands/oc-review-pr.md"
      provides: "Renamed review-pr command"
    - path: "assets/commands/oc-update-docs.md"
      provides: "Renamed update-docs command"
    - path: "assets/commands/oc-new-agent.md"
      provides: "Renamed new-agent command"
    - path: "assets/commands/oc-new-skill.md"
      provides: "Renamed new-skill command"
    - path: "assets/commands/oc-new-command.md"
      provides: "Renamed new-command command"
    - path: "src/installer.ts"
      provides: "Updated DEPRECATED_ASSETS and FORCE_UPDATE_ASSETS"
      contains: "commands/brainstorm.md"
  key_links:
    - from: "src/installer.ts"
      to: "assets/commands/"
      via: "DEPRECATED_ASSETS array drives cleanup of old names on user machines"
      pattern: "commands/brainstorm\\.md"
---

<objective>
Rename all 10 plugin command files from unprefixed to oc- prefixed names, delete oc-configure.md, and update the installer's DEPRECATED_ASSETS and FORCE_UPDATE_ASSETS arrays.

Purpose: Establishes the oc- namespace prefix for all plugin commands (BFIX-02) and removes the oc-configure slash command (BFIX-03). Users upgrading from v3 get seamless migration via the DEPRECATED_ASSETS cleanup.
Output: 10 renamed command files, 1 deleted file, updated installer.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-namespace-cleanup/18-CONTEXT.md
@src/installer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename command files and delete oc-configure</name>
  <files>
    assets/commands/oc-brainstorm.md
    assets/commands/oc-new-agent.md
    assets/commands/oc-new-command.md
    assets/commands/oc-new-skill.md
    assets/commands/oc-quick.md
    assets/commands/oc-review-pr.md
    assets/commands/oc-stocktake.md
    assets/commands/oc-tdd.md
    assets/commands/oc-update-docs.md
    assets/commands/oc-write-plan.md
    assets/commands/oc-configure.md
  </files>
  <read_first>
    assets/commands/brainstorm.md
    assets/commands/new-agent.md
    assets/commands/new-command.md
    assets/commands/new-skill.md
    assets/commands/quick.md
    assets/commands/review-pr.md
    assets/commands/stocktake.md
    assets/commands/tdd.md
    assets/commands/update-docs.md
    assets/commands/write-plan.md
    assets/commands/oc-configure.md
  </read_first>
  <action>
    Per D-01, D-02: Use `git mv` to rename each command file to its oc- prefixed name:
    - `git mv assets/commands/brainstorm.md assets/commands/oc-brainstorm.md`
    - `git mv assets/commands/new-agent.md assets/commands/oc-new-agent.md`
    - `git mv assets/commands/new-command.md assets/commands/oc-new-command.md`
    - `git mv assets/commands/new-skill.md assets/commands/oc-new-skill.md`
    - `git mv assets/commands/quick.md assets/commands/oc-quick.md`
    - `git mv assets/commands/review-pr.md assets/commands/oc-review-pr.md`
    - `git mv assets/commands/stocktake.md assets/commands/oc-stocktake.md`
    - `git mv assets/commands/tdd.md assets/commands/oc-tdd.md`
    - `git mv assets/commands/update-docs.md assets/commands/oc-update-docs.md`
    - `git mv assets/commands/write-plan.md assets/commands/oc-write-plan.md`

    Per D-03: Delete oc-configure.md entirely:
    - `git rm assets/commands/oc-configure.md`

    After renaming, the assets/commands/ directory should contain exactly 10 files, all prefixed with `oc-`.
  </action>
  <verify>
    <automated>ls assets/commands/ | grep -v .gitkeep | sort</automated>
  </verify>
  <acceptance_criteria>
    - `ls assets/commands/` shows exactly: oc-brainstorm.md, oc-new-agent.md, oc-new-command.md, oc-new-skill.md, oc-quick.md, oc-review-pr.md, oc-stocktake.md, oc-tdd.md, oc-update-docs.md, oc-write-plan.md
    - `ls assets/commands/brainstorm.md 2>&1` returns "No such file or directory"
    - `ls assets/commands/oc-configure.md 2>&1` returns "No such file or directory"
    - Each renamed file preserves its original content (frontmatter + body unchanged)
  </acceptance_criteria>
  <done>All 10 command files renamed with oc- prefix, oc-configure.md deleted, no old unprefixed files remain</done>
</task>

<task type="auto">
  <name>Task 2: Update installer DEPRECATED_ASSETS and FORCE_UPDATE_ASSETS</name>
  <files>src/installer.ts</files>
  <read_first>src/installer.ts</read_first>
  <action>
    Per D-01 and D-03, update `src/installer.ts`:

    1. Expand `DEPRECATED_ASSETS` to include all 10 old unprefixed command paths AND `commands/oc-configure.md`. The final array must be:
    ```typescript
    const DEPRECATED_ASSETS = [
      "agents/placeholder-agent.md",
      "commands/configure.md",
      "commands/oc-configure.md",
      "commands/brainstorm.md",
      "commands/new-agent.md",
      "commands/new-command.md",
      "commands/new-skill.md",
      "commands/quick.md",
      "commands/review-pr.md",
      "commands/stocktake.md",
      "commands/tdd.md",
      "commands/update-docs.md",
      "commands/write-plan.md",
    ] as const;
    ```

    2. Clear `FORCE_UPDATE_ASSETS` since oc-configure.md no longer exists in the bundle. The array becomes empty:
    ```typescript
    const FORCE_UPDATE_ASSETS = [] as const;
    ```

    3. Keep all existing functions (cleanupDeprecatedAssets, forceUpdateAssets, etc.) unchanged — they already handle empty arrays correctly.
  </action>
  <verify>
    <automated>bun test tests/installer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/installer.ts` contains `"commands/brainstorm.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/oc-configure.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/quick.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/tdd.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/write-plan.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/new-agent.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/new-skill.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/new-command.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/review-pr.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/stocktake.md"` in DEPRECATED_ASSETS
    - `src/installer.ts` contains `"commands/update-docs.md"` in DEPRECATED_ASSETS
    - FORCE_UPDATE_ASSETS is `[] as const` (empty array)
    - `bun test tests/installer.test.ts` passes (test may need updating if it references old file names — see note below)
    - NOTE: The installer test at line 64 references `commands/new-agent.md` — this test creates its own source files, so it will still work as-is because the test uses its own temp directory. However, if the test DOES fail, update the test to use `commands/oc-new-agent.md` as the test fixture name.
  </acceptance_criteria>
  <done>DEPRECATED_ASSETS contains all 12 entries (2 existing + 10 old command names), FORCE_UPDATE_ASSETS is empty, installer test passes</done>
</task>

</tasks>

<verification>
- `ls assets/commands/ | wc -l` returns 10 (all oc- prefixed)
- `grep -c "commands/" src/installer.ts` shows 12+ lines in DEPRECATED_ASSETS
- `grep "FORCE_UPDATE_ASSETS" src/installer.ts` shows empty array
- `bun test tests/installer.test.ts` passes
</verification>

<success_criteria>
- All 10 command files exist with oc- prefix in assets/commands/
- No unprefixed command files remain in assets/commands/
- oc-configure.md is deleted
- DEPRECATED_ASSETS lists all old names for user machine cleanup
- FORCE_UPDATE_ASSETS is empty
- Installer test passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-namespace-cleanup/18-01-SUMMARY.md`
</output>
