---
phase: 01-plugin-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/installer.ts
  - src/config.ts
  - src/index.ts
  - assets/commands/configure.md
  - assets/agents/placeholder-agent.md
  - tests/installer.test.ts
  - tests/config.test.ts
autonomous: true
requirements:
  - PLGN-03
  - PLGN-04

must_haves:
  truths:
    - "Plugin copies missing asset files from assets/ to ~/.config/opencode/ on load"
    - "Plugin skips copying files that already exist in the target directory"
    - "Plugin handles skills subdirectory structure (skills/<name>/SKILL.md)"
    - "Config file tracks whether plugin has been configured"
    - "Agents are written without a model field by default (model-agnostic)"
    - "Config wizard state is persisted to ~/.config/opencode/opencode-assets.json"
    - "A /configure command exists as a bundled asset"
  artifacts:
    - path: "src/installer.ts"
      provides: "Asset installation logic with no-clobber strategy"
      exports: ["installAssets"]
      min_lines: 40
    - path: "src/config.ts"
      provides: "Config file management and first-load detection"
      exports: ["loadConfig", "saveConfig", "isFirstLoad", "CONFIG_PATH", "PluginConfig"]
    - path: "assets/commands/configure.md"
      provides: "The /configure command that instructs LLM to call oc_configure tool"
      contains: "oc_configure"
    - path: "assets/agents/placeholder-agent.md"
      provides: "A minimal agent to verify asset installation works"
      contains: "description:"
  key_links:
    - from: "src/index.ts"
      to: "src/installer.ts"
      via: "import installAssets, called on plugin load"
      pattern: "import.*installer"
    - from: "src/index.ts"
      to: "src/config.ts"
      via: "import loadConfig/isFirstLoad, called on plugin load"
      pattern: "import.*config"
    - from: "src/installer.ts"
      to: "src/utils/fs-helpers.ts"
      via: "import fileExists, copyIfMissing, ensureDir"
      pattern: "import.*fs-helpers"
    - from: "src/installer.ts"
      to: "src/utils/paths.ts"
      via: "import getGlobalConfigDir, getAssetsDir"
      pattern: "import.*paths"
---

<objective>
Implement the asset installer (no-clobber copy from assets/ to ~/.config/opencode/) and configuration system (first-load detection, model mapping config file) so the plugin self-heals missing assets on every load and supports model-agnostic agents.

Purpose: Fulfill PLGN-03 (bundled assets installed to correct paths) and PLGN-04 (works with any provider -- model-agnostic agents with optional config wizard).
Output: Working installer, config module, bundled /configure command, a placeholder agent asset, and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-infrastructure/01-CONTEXT.md
@.planning/phases/01-plugin-infrastructure/01-RESEARCH.md
@.planning/phases/01-plugin-infrastructure/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From src/utils/paths.ts:
```typescript
export function getGlobalConfigDir(): string  // Returns ~/.config/opencode
export function getAssetsDir(): string         // Returns package's assets/ directory
```

From src/utils/fs-helpers.ts:
```typescript
export function fileExists(path: string): Promise<boolean>
export function ensureDir(dirPath: string): Promise<void>
export function copyIfMissing(source: string, target: string): Promise<{ copied: boolean }>
```

From src/index.ts:
```typescript
import type { Plugin } from "@opencode-ai/plugin"
// Currently registers only oc_placeholder tool
// This plan will add installAssets() and loadConfig() calls
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement config module and asset installer</name>
  <files>src/config.ts, src/installer.ts, tests/config.test.ts, tests/installer.test.ts</files>
  <read_first>
    src/utils/paths.ts (getGlobalConfigDir, getAssetsDir signatures)
    src/utils/fs-helpers.ts (fileExists, copyIfMissing, ensureDir signatures)
    .planning/phases/01-plugin-infrastructure/01-RESEARCH.md (Pattern 2 at line ~169, Pattern 3 at line ~197, installer code at line ~352, Pitfall 4 at line ~297, Pitfall 6 at line ~315)
  </read_first>
  <behavior>
    - loadConfig() returns null when config file does not exist
    - loadConfig() returns PluginConfig object when file exists with valid JSON
    - saveConfig() writes JSON to CONFIG_PATH and creates parent directory
    - isFirstLoad(null) returns true
    - isFirstLoad({ configured: false, ... }) returns true
    - isFirstLoad({ configured: true, ... }) returns false
    - installAssets() copies files from assets/agents/ to target/agents/
    - installAssets() copies skill directories from assets/skills/<name>/ to target/skills/<name>/
    - installAssets() copies files from assets/commands/ to target/commands/
    - installAssets() skips files that already exist at target
    - installAssets() returns { copied: string[], skipped: string[], errors: string[] }
    - installAssets() creates target directories if they do not exist
  </behavior>
  <action>
    1. Create `src/config.ts`:
       - Define and export `PluginConfig` interface:
         ```typescript
         export interface PluginConfig {
           version: 1
           configured: boolean
           models: Record<string, string> // agent-name -> "provider/model-id"
         }
         ```
       - Export `CONFIG_PATH`: `path.join(os.homedir(), ".config", "opencode", "opencode-assets.json")` (per research recommendation, Claude's discretion on config location).
       - Export `async function loadConfig(): Promise<PluginConfig | null>` -- reads CONFIG_PATH with `fs.readFile`, parses JSON, returns null if file missing (catch ENOENT). Returns parsed `PluginConfig` on success.
       - Export `async function saveConfig(config: PluginConfig): Promise<void>` -- calls `ensureDir(dirname(CONFIG_PATH))`, then writes JSON.stringify(config, null, 2) to CONFIG_PATH.
       - Export `function isFirstLoad(config: PluginConfig | null): boolean` -- returns `config === null || !config.configured`.
       - Export `function createDefaultConfig(): PluginConfig` -- returns `{ version: 1, configured: false, models: {} }`.

    2. Create `src/installer.ts`:
       - Import `getGlobalConfigDir`, `getAssetsDir` from `./utils/paths`.
       - Import `fileExists`, `copyIfMissing`, `ensureDir` from `./utils/fs-helpers`.
       - Import `readdir` from `node:fs/promises`, `join` from `node:path`.
       - Define `InstallResult` type: `{ copied: string[], skipped: string[], errors: string[] }`.
       - Export `async function installAssets(assetsDir?: string, targetDir?: string): Promise<InstallResult>`:
         - Default `assetsDir` to `getAssetsDir()`, `targetDir` to `getGlobalConfigDir()` (per D-05, global default).
         - Process agents: read `assetsDir/agents/`, for each `.md` file, `copyIfMissing` to `targetDir/agents/<filename>`. Skip `.gitkeep`.
         - Process commands: same pattern for `assetsDir/commands/`.
         - Process skills: read `assetsDir/skills/`, for each subdirectory, read files inside, `copyIfMissing` to `targetDir/skills/<dirname>/<filename>`. Skills are directories not individual files (per research: `skills/<name>/SKILL.md`).
         - Wrap each copy operation in try/catch. On error (EACCES, EROFS etc.), push descriptive message to `errors` array. Do NOT throw -- continue processing remaining assets (per research Pitfall 4).
         - Return `{ copied, skipped, errors }`.

    3. Write tests (RED first, then GREEN):
       - `tests/config.test.ts`:
         - Test `isFirstLoad(null)` returns `true`
         - Test `isFirstLoad({ version: 1, configured: false, models: {} })` returns `true`
         - Test `isFirstLoad({ version: 1, configured: true, models: {} })` returns `false`
         - Test `createDefaultConfig()` returns object with `version: 1` and `configured: false`
         - Test `saveConfig` + `loadConfig` round-trip using temp directory (override CONFIG_PATH or pass path)
       - `tests/installer.test.ts`:
         - Create temp dirs for assets source and target
         - Write a fake agent .md file to source/agents/
         - Call `installAssets(sourceDir, targetDir)` -- verify file copied to target/agents/
         - Call again -- verify file skipped on second call
         - Create a source/skills/test-skill/SKILL.md file
         - Call `installAssets` -- verify skill directory and file copied
         - Verify .gitkeep files are NOT copied

    4. Run `bun test` -- all tests must pass.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-assets && bun test tests/config.test.ts tests/installer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/config.ts` exports `PluginConfig`, `CONFIG_PATH`, `loadConfig`, `saveConfig`, `isFirstLoad`, `createDefaultConfig`
    - `src/config.ts` contains `"opencode-assets.json"` in CONFIG_PATH
    - `src/config.ts` contains `configured: boolean` in PluginConfig interface
    - `src/config.ts` contains `models: Record<string, string>` in PluginConfig interface
    - `src/installer.ts` exports `installAssets`
    - `src/installer.ts` contains `copyIfMissing` call
    - `src/installer.ts` handles "agents", "commands", and "skills" asset types
    - `src/installer.ts` contains `.gitkeep` skip logic
    - `src/installer.ts` contains try/catch around copy operations
    - `bun test tests/config.test.ts tests/installer.test.ts` exits with code 0
  </acceptance_criteria>
  <done>Config module persists/loads plugin configuration with first-load detection. Installer copies assets with no-clobber strategy, handles all three asset types including skill subdirectories, and gracefully handles errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create bundled assets, wire installer into plugin entry, and add event hook</name>
  <files>src/index.ts, assets/agents/placeholder-agent.md, assets/commands/configure.md, tests/index.test.ts</files>
  <read_first>
    src/index.ts (current state from Plan 01)
    src/installer.ts (just created -- installAssets signature)
    src/config.ts (just created -- loadConfig, isFirstLoad signatures)
    .planning/phases/01-plugin-infrastructure/01-RESEARCH.md (agent frontmatter at line ~427, command format, Pattern 1 at line ~125, open question 3 at line ~483)
  </read_first>
  <action>
    1. Create `assets/agents/placeholder-agent.md` -- a minimal agent to verify the asset installation pipeline works end-to-end (per D-10, strict permissions -- this is a read-only placeholder):
       ```markdown
       ---
       description: A placeholder agent installed by opencode-assets to verify the plugin is working
       mode: all
       permission:
         read: allow
         edit: deny
         bash: deny
         webfetch: deny
         task: deny
       ---
       You are a placeholder agent installed by the opencode-assets plugin. Your purpose is to confirm the plugin's asset installation is working correctly.

       When invoked, explain that you are a placeholder and suggest the user explore other agents provided by the opencode-assets plugin.
       ```
       Note: No `model` field -- agent is model-agnostic (per D-08, PLGN-04).

    2. Create `assets/commands/configure.md` -- the /configure command (per D-08, open question 3 in research: command markdown instructs LLM to call the tool):
       ```markdown
       ---
       description: Configure the opencode-assets plugin -- assign models to agents
       ---
       Run the `oc_configure` tool to start the opencode-assets configuration wizard. This lets you assign specific models to each agent provided by the plugin.

       If no arguments are needed, call `oc_configure` with no parameters. The tool will guide you through the configuration process.
       ```

    3. Update `src/index.ts` to wire in the installer and config (per D-02, D-03):
       ```typescript
       import type { Plugin } from "@opencode-ai/plugin"
       import { ocPlaceholder } from "./tools/placeholder"
       import { installAssets } from "./installer"
       import { loadConfig, isFirstLoad } from "./config"

       const plugin: Plugin = async (ctx) => {
         // Self-healing asset installation on every load (D-03)
         const installResult = await installAssets()

         // Load config for first-load detection (per Pitfall 6: load before returning hooks)
         const config = await loadConfig()

         return {
           tool: {
             oc_placeholder: ocPlaceholder,
           },
           event: async ({ event }) => {
             if (event.type === "session.created" && isFirstLoad(config)) {
               // First load: config wizard will be triggered via /configure command
               // For now, this is a detection hook -- Phase 2 will add the oc_configure tool
             }
           },
         }
       }

       export default plugin
       ```

    4. Update `tests/index.test.ts` to verify:
       - Default export is an async function
       - Calling plugin with mock context returns object with `tool` property
       - `tool.oc_placeholder` exists on the returned object
       - Return value has `event` property (function)
       - Note: Use a mock/temp directory for the test to avoid actually writing to ~/.config/opencode/. Pass environment or use `installAssets(assetsDir, tmpTargetDir)` pattern -- but since index.ts calls `installAssets()` with defaults, mock the fs operations or set up a test harness. Simplest approach: verify the plugin function does not throw when assets dir is empty.

    5. Verify bundled asset files exist:
       - `assets/agents/placeholder-agent.md` contains `description:` in frontmatter
       - `assets/commands/configure.md` contains `oc_configure`

    6. Run `bun test` -- all tests must pass.

    7. Remove any `.gitkeep` from `assets/agents/` and `assets/commands/` since they now have real files. Keep `.gitkeep` in `assets/skills/` (no skills yet).
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-assets && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `assets/agents/placeholder-agent.md` exists and contains `description:` on a line
    - `assets/agents/placeholder-agent.md` does NOT contain `model:` (model-agnostic per PLGN-04)
    - `assets/agents/placeholder-agent.md` contains `edit: deny` (per D-10)
    - `assets/commands/configure.md` exists and contains `oc_configure`
    - `src/index.ts` contains `import { installAssets }` from installer
    - `src/index.ts` contains `import { loadConfig, isFirstLoad }` from config
    - `src/index.ts` contains `await installAssets()`
    - `src/index.ts` contains `await loadConfig()`
    - `src/index.ts` contains `event.type === "session.created"`
    - `src/index.ts` contains `isFirstLoad(config)`
    - `bun test` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>Plugin entry point calls installAssets() on every load (self-healing per D-03), loads config for first-load detection, includes event hook for session.created. Bundled assets include a model-agnostic placeholder agent and a /configure command. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun test` passes all test files
- `assets/agents/placeholder-agent.md` has no `model:` field (PLGN-04 model-agnostic)
- `assets/commands/configure.md` references `oc_configure` tool
- `src/installer.ts` copies from assets/ to ~/.config/opencode/ with no-clobber
- `src/config.ts` manages plugin configuration at ~/.config/opencode/opencode-assets.json
- `src/index.ts` calls installAssets() and loadConfig() before returning tool registrations
- Plugin load sequence: install assets -> load config -> return tools + event hook
</verification>

<success_criteria>
- Bundled assets are copied to correct filesystem paths on plugin load (PLGN-03)
- Existing user files are never overwritten (D-04)
- Agents have no hardcoded model field -- works with any provider (PLGN-04)
- Config system detects first load for future configuration wizard
- /configure command exists as a bundled command asset
- All tests pass with `bun test`
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-infrastructure/01-02-SUMMARY.md`
</output>
