---
phase: 01-plugin-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - src/index.ts
  - src/tools/placeholder.ts
  - src/utils/paths.ts
  - src/utils/fs-helpers.ts
  - tests/utils/paths.test.ts
  - tests/utils/fs-helpers.test.ts
  - tests/tools/placeholder.test.ts
autonomous: true
requirements:
  - PLGN-01
  - PLGN-02

must_haves:
  truths:
    - "Running `bun install` in the project root succeeds without errors"
    - "Importing the default export from src/index.ts returns a valid Plugin function"
    - "The plugin registers an `oc_placeholder` tool that accepts a string argument and returns a response"
    - "Utility functions resolve global and project paths correctly"
    - "All tests pass with `bun test`"
  artifacts:
    - path: "package.json"
      provides: "npm package definition with correct main, files, dependencies"
      contains: "opencode-assets"
    - path: "src/index.ts"
      provides: "Plugin entry point with default export"
      exports: ["default"]
    - path: "src/tools/placeholder.ts"
      provides: "Placeholder tool proving registration works"
      exports: ["ocPlaceholder"]
    - path: "src/utils/paths.ts"
      provides: "Path resolution utilities for global and project targets"
      exports: ["getGlobalConfigDir", "getAssetsDir"]
    - path: "src/utils/fs-helpers.ts"
      provides: "Filesystem utilities: fileExists, copyIfMissing, ensureDir"
      exports: ["fileExists", "copyIfMissing", "ensureDir"]
  key_links:
    - from: "src/index.ts"
      to: "src/tools/placeholder.ts"
      via: "import ocPlaceholder"
      pattern: "import.*placeholder"
    - from: "src/index.ts"
      to: "@opencode-ai/plugin"
      via: "Plugin type and tool helper"
      pattern: "import.*@opencode-ai/plugin"
---

<objective>
Scaffold the npm package structure and register a placeholder tool with OpenCode to prove the plugin loads and registers tools correctly.

Purpose: Establish the project foundation (PLGN-01) and validate tool registration with Zod schemas (PLGN-02) so that subsequent plans can build on a working plugin.
Output: A runnable Bun project with package.json, TypeScript config, Biome config, utility modules, plugin entry point, and a registered placeholder tool with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-infrastructure/01-CONTEXT.md
@.planning/phases/01-plugin-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold npm package and utility modules</name>
  <files>package.json, tsconfig.json, biome.json, src/utils/paths.ts, src/utils/fs-helpers.ts, tests/utils/paths.test.ts, tests/utils/fs-helpers.test.ts</files>
  <read_first>
    .planning/phases/01-plugin-infrastructure/01-RESEARCH.md (package.json example at line ~396, architecture pattern at line ~99, installation commands at line ~84)
    CLAUDE.md (project constraints)
  </read_first>
  <behavior>
    - getGlobalConfigDir() returns path ending in ".config/opencode"
    - getAssetsDir() returns path ending in "assets" relative to package root
    - fileExists() returns true for existing files, false for non-existent
    - ensureDir() creates nested directories without error
    - copyIfMissing() copies file when target missing, skips when target exists
    - copyIfMissing() returns { copied: true } when file was copied
    - copyIfMissing() returns { copied: false } when file was skipped (already exists)
  </behavior>
  <action>
    1. Initialize project with `bun init` (accept defaults, then overwrite files).

    2. Create `package.json` with these exact values (per D-01, PLGN-01):
       ```json
       {
         "name": "opencode-assets",
         "version": "0.1.0",
         "type": "module",
         "main": "src/index.ts",
         "files": ["src/", "assets/"],
         "scripts": {
           "test": "bun test",
           "lint": "biome check .",
           "format": "biome format . --write"
         },
         "peerDependencies": {
           "@opencode-ai/plugin": ">=1.3.0"
         },
         "devDependencies": {
           "@opencode-ai/plugin": "^1.3.8",
           "@types/bun": "^1.3.11",
           "@biomejs/biome": "^2.4.10"
         },
         "dependencies": {
           "yaml": "^2.8.3"
         }
       }
       ```

    3. Create `tsconfig.json`:
       ```json
       {
         "compilerOptions": {
           "target": "ESNext",
           "module": "ESNext",
           "moduleResolution": "bundler",
           "strict": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "outDir": "dist",
           "rootDir": ".",
           "types": ["bun-types"]
         },
         "include": ["src/**/*.ts", "tests/**/*.ts"]
       }
       ```

    4. Create `biome.json`:
       ```json
       {
         "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
         "organizeImports": { "enabled": true },
         "linter": { "enabled": true },
         "formatter": {
           "enabled": true,
           "indentStyle": "tab",
           "lineWidth": 100
         }
       }
       ```

    5. Run `bun install` to install all dependencies.

    6. Create `src/utils/paths.ts` with two exported functions:
       - `getGlobalConfigDir()`: Returns `path.join(os.homedir(), ".config", "opencode")` using `node:path` and `node:os`.
       - `getAssetsDir()`: Returns `path.join(import.meta.dir, "..", "..", "assets")` -- resolves the `assets/` directory relative to the package root. Use `import.meta.dir` (Bun-native, per research Pitfall 5).

    7. Create `src/utils/fs-helpers.ts` with three exported functions:
       - `fileExists(path: string): Promise<boolean>` -- uses `access()` from `node:fs/promises`, returns true/false.
       - `ensureDir(dirPath: string): Promise<void>` -- calls `mkdir(dirPath, { recursive: true })`.
       - `copyIfMissing(source: string, target: string): Promise<{ copied: boolean }>` -- checks `fileExists(target)`, if exists returns `{ copied: false }`, otherwise calls `ensureDir(dirname(target))` then `copyFile(source, target)` and returns `{ copied: true }`.

    8. Write tests (RED first, then GREEN):
       - `tests/utils/paths.test.ts`: Test that `getGlobalConfigDir()` ends with `.config/opencode`, test that `getAssetsDir()` ends with `assets`.
       - `tests/utils/fs-helpers.test.ts`: Test `fileExists` with existing file (use `import.meta.path`) and non-existent path. Test `ensureDir` creates a temp dir. Test `copyIfMissing` copies when missing and skips when existing (use temp directories with `node:os` `tmpdir()`).

    9. Run `bun test` -- all tests must pass.

    10. Create empty directories: `assets/agents/`, `assets/skills/`, `assets/commands/` with `.gitkeep` files.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-assets && bun test tests/utils/</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "opencode-assets"` and `"main": "src/index.ts"` and `"type": "module"`
    - `package.json` contains `"@opencode-ai/plugin"` in both peerDependencies and devDependencies
    - `package.json` contains `"yaml"` in dependencies
    - `tsconfig.json` contains `"module": "ESNext"` and `"strict": true`
    - `biome.json` exists and contains `"linter"` key
    - `src/utils/paths.ts` exports `getGlobalConfigDir` and `getAssetsDir`
    - `src/utils/fs-helpers.ts` exports `fileExists`, `copyIfMissing`, `ensureDir`
    - `node_modules/` directory exists (bun install succeeded)
    - `bun test tests/utils/` exits with code 0
    - `assets/agents/.gitkeep`, `assets/skills/.gitkeep`, `assets/commands/.gitkeep` exist
  </acceptance_criteria>
  <done>Project scaffolded with all config files, utility modules implemented and tested, dependencies installed, asset directories created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create placeholder tool and plugin entry point</name>
  <files>src/tools/placeholder.ts, src/index.ts, tests/tools/placeholder.test.ts, tests/index.test.ts</files>
  <read_first>
    src/utils/paths.ts (just created -- import patterns)
    src/utils/fs-helpers.ts (just created -- import patterns)
    .planning/phases/01-plugin-infrastructure/01-RESEARCH.md (Pattern 1 at line ~125, Pattern 4 at line ~219, complete plugin example at line ~326)
  </read_first>
  <behavior>
    - ocPlaceholder tool returns a string containing the input message
    - ocPlaceholder tool returns a string containing "OpenCode Assets"
    - Plugin default export is an async function
    - Plugin return value has a `tool` property containing `oc_placeholder`
  </behavior>
  <action>
    1. Create `src/tools/placeholder.ts` (per D-06, PLGN-02):
       ```typescript
       import { tool } from "@opencode-ai/plugin"

       export const ocPlaceholder = tool({
         description: "Verifies that the OpenCode Assets plugin is loaded and working",
         args: {
           message: tool.schema.string().describe("A test message to echo back"),
         },
         async execute(args) {
           return `OpenCode Assets plugin is active. Your message: ${args.message}`
         },
       })
       ```

    2. Create `src/index.ts` (per D-02 -- single default export):
       ```typescript
       import type { Plugin } from "@opencode-ai/plugin"
       import { ocPlaceholder } from "./tools/placeholder"

       const plugin: Plugin = async (ctx) => {
         return {
           tool: {
             oc_placeholder: ocPlaceholder,
           },
         }
       }

       export default plugin
       ```
       Note: Asset installation and config loading will be added in Plan 02. Keep entry point minimal for now.

    3. Write tests (RED first, then GREEN):
       - `tests/tools/placeholder.test.ts`: Import `ocPlaceholder`, call `ocPlaceholder.execute({ message: "hello" })` (or test the tool's properties depending on the tool() return type -- verify the tool object has `description`, `args`, and `execute` properties).
       - `tests/index.test.ts`: Import default from `src/index.ts`, verify it is a function, call it with a mock context `{ directory: "/tmp", worktree: "/tmp" }`, verify the return value has a `tool` property, verify `tool.oc_placeholder` exists.

    4. Run `bun test` -- all tests must pass.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-assets && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/placeholder.ts` contains `export const ocPlaceholder`
    - `src/tools/placeholder.ts` contains `tool.schema.string()` (Zod validation per PLGN-02)
    - `src/tools/placeholder.ts` contains `description:` string
    - `src/index.ts` contains `export default plugin`
    - `src/index.ts` contains `oc_placeholder:` (tool name with oc_ prefix per D-06)
    - `src/index.ts` imports from `"@opencode-ai/plugin"` (Plugin type)
    - `src/index.ts` imports from `"./tools/placeholder"`
    - `bun test` exits with code 0 (all test files pass)
  </acceptance_criteria>
  <done>Plugin entry point exports a valid Plugin function, placeholder tool is registered with oc_ prefix and Zod schema, all tests pass</done>
</task>

</tasks>

<verification>
- `bun test` passes all test files (utils + tools + index)
- `bun install` completes without errors
- `package.json` has correct structure for npm distribution (name, main, files, type)
- Plugin can be imported and called: `const plugin = (await import("./src/index.ts")).default; const result = await plugin({ directory: "/tmp", worktree: "/tmp" });`
- Result contains `tool.oc_placeholder` with execute function
</verification>

<success_criteria>
- Project is a valid Bun/TypeScript package with all dependencies installed
- Plugin registers at least one tool (oc_placeholder) with Zod-validated schema
- Utility modules (paths, fs-helpers) are tested and working
- Package can be referenced from opencode.json as `"opencode-assets"`
- All tests pass with `bun test`
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-infrastructure/01-01-SUMMARY.md`
</output>
