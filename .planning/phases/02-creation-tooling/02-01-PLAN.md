---
phase: 02-creation-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/utils/validators.ts
  - src/templates/agent-template.ts
  - src/templates/skill-template.ts
  - src/templates/command-template.ts
  - tests/validators.test.ts
  - tests/templates.test.ts
autonomous: true
requirements: [CRTL-04, CRTL-05]

must_haves:
  truths:
    - "Skill names are validated against regex ^[a-z0-9]+(-[a-z0-9]+)*$ with 1-64 char limit"
    - "Agent names are validated for filesystem safety using the same skill regex"
    - "Command names are rejected if they match built-in commands (init, undo, redo, share, help, config, compact, clear, cost, login, logout, bug)"
    - "YAML frontmatter is generated correctly for agents, skills, and commands using the yaml package"
    - "Templates produce valid markdown with correct frontmatter structure"
  artifacts:
    - path: "src/utils/validators.ts"
      provides: "Name validation for agents, skills, commands"
      exports: ["validateAssetName", "validateCommandName", "BUILT_IN_COMMANDS", "ASSET_NAME_REGEX"]
    - path: "src/templates/agent-template.ts"
      provides: "Agent markdown generation with YAML frontmatter"
      exports: ["generateAgentMarkdown", "AgentTemplateInput"]
    - path: "src/templates/skill-template.ts"
      provides: "Skill SKILL.md generation with YAML frontmatter"
      exports: ["generateSkillMarkdown", "SkillTemplateInput"]
    - path: "src/templates/command-template.ts"
      provides: "Command markdown generation with YAML frontmatter"
      exports: ["generateCommandMarkdown", "CommandTemplateInput"]
  key_links:
    - from: "src/templates/*.ts"
      to: "yaml"
      via: "import { stringify } from 'yaml'"
      pattern: "stringify\\("
    - from: "src/utils/validators.ts"
      to: "src/templates/*.ts"
      via: "validation before template generation (consumed by tools in Plan 02)"
      pattern: "validateAssetName|validateCommandName"
---

<objective>
Create shared validation utilities and template generation functions for the three creation tools.

Purpose: Establish the pure-function foundation (validators + templates) that Plan 02's tool implementations will consume. Separating these enables thorough TDD testing of validation logic and template output without filesystem side effects.

Output: `src/utils/validators.ts`, `src/templates/agent-template.ts`, `src/templates/skill-template.ts`, `src/templates/command-template.ts`, plus comprehensive tests for all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-creation-tooling/02-CONTEXT.md
@.planning/phases/02-creation-tooling/02-RESEARCH.md

<interfaces>
<!-- Existing utilities the executor needs -->

From src/utils/fs-helpers.ts:
```typescript
export function fileExists(path: string): Promise<boolean>;
export function ensureDir(dirPath: string): Promise<void>;
```

From src/utils/paths.ts:
```typescript
export function getGlobalConfigDir(): string;
export function getAssetsDir(): string;
```

From src/tools/placeholder.ts (tool registration pattern):
```typescript
import { tool } from "@opencode-ai/plugin";
export const ocPlaceholder = tool({
  description: "...",
  args: { message: tool.schema.string().max(1000).describe("...") },
  async execute(args) { return `...`; },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validators and yaml dependency</name>
  <files>package.json, src/utils/validators.ts, tests/validators.test.ts</files>
  <read_first>
    - src/utils/fs-helpers.ts (existing utility pattern to follow)
    - src/utils/paths.ts (existing utility pattern)
    - package.json (current dependencies)
    - .planning/phases/02-creation-tooling/02-RESEARCH.md (validation specs, built-in command list)
  </read_first>
  <behavior>
    - validateAssetName("my-agent") returns { valid: true }
    - validateAssetName("") returns { valid: false, error: "..." } (too short)
    - validateAssetName("a".repeat(65)) returns { valid: false, error: "..." } (too long for skills)
    - validateAssetName("My Agent") returns { valid: false, error: "..." } (uppercase/spaces)
    - validateAssetName("--bad") returns { valid: false, error: "..." } (leading hyphen)
    - validateAssetName("bad-") returns { valid: false, error: "..." } (trailing hyphen)
    - validateAssetName("bad--name") returns { valid: false, error: "..." } (consecutive hyphens)
    - validateCommandName("help") returns { valid: false, error: "...built-in..." }
    - validateCommandName("init") returns { valid: false, error: "...built-in..." }
    - validateCommandName("my-cmd") returns { valid: true }
    - validateCommandName("bad/name") returns { valid: false, error: "...filesystem..." }
  </behavior>
  <action>
    1. Install `yaml` package: `bun add yaml`

    2. Create `src/utils/validators.ts` with:
       - `ASSET_NAME_REGEX = /^[a-z0-9]+(-[a-z0-9]+)*$/` (per D-05, matches OpenCode's skill name regex)
       - `MAX_NAME_LENGTH = 64` (per D-05)
       - `BUILT_IN_COMMANDS = new Set(["init", "undo", "redo", "share", "help", "config", "compact", "clear", "cost", "login", "logout", "bug"])` (per D-09)
       - `FILESYSTEM_UNSAFE_REGEX = /[<>:"/\\|?*\x00-\x1f]/`

       Export two functions:
       - `validateAssetName(name: string): { readonly valid: boolean; readonly error?: string }` — Validates name length (1-64), regex match. Used for agents, skills, and commands. Returns immutable result object (per coding-style). Error messages must be actionable, e.g.: "Name 'My Agent' is invalid. Names must be 1-64 lowercase alphanumeric characters with hyphens (e.g., 'my-agent')."
       - `validateCommandName(name: string): { readonly valid: boolean; readonly error?: string }` — Calls validateAssetName first, then additionally checks against BUILT_IN_COMMANDS set and filesystem-unsafe chars (per D-09). Error: "Command name 'help' conflicts with a built-in OpenCode command. Choose a different name."

    3. Create `tests/validators.test.ts` with tests for all behavior cases above. Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>bun test tests/validators.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ASSET_NAME_REGEX" src/utils/validators.ts
    - grep -q "BUILT_IN_COMMANDS" src/utils/validators.ts
    - grep -q "validateAssetName" src/utils/validators.ts
    - grep -q "validateCommandName" src/utils/validators.ts
    - grep -q '"yaml"' package.json
    - bun test tests/validators.test.ts passes with 0 failures
  </acceptance_criteria>
  <done>
    - validateAssetName rejects empty, too-long, uppercase, spaces, leading/trailing/consecutive hyphens
    - validateAssetName accepts valid lowercase-hyphenated names 1-64 chars
    - validateCommandName rejects all 12 built-in commands and filesystem-unsafe characters
    - yaml package is installed as a runtime dependency in package.json
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template generation functions</name>
  <files>src/templates/agent-template.ts, src/templates/skill-template.ts, src/templates/command-template.ts, tests/templates.test.ts</files>
  <read_first>
    - src/utils/validators.ts (just created in Task 1)
    - .planning/phases/02-creation-tooling/02-RESEARCH.md (exact frontmatter specs, code examples)
    - assets/agents/placeholder-agent.md (existing agent format reference)
    - assets/commands/configure.md (existing command format reference)
  </read_first>
  <behavior>
    - generateAgentMarkdown({ name: "code-reviewer", description: "Reviews code", mode: "subagent" }) produces markdown starting with "---\n" containing "description: Reviews code" and "mode: subagent" and default read-only permissions
    - generateAgentMarkdown with optional model field includes "model: ..." in frontmatter
    - generateAgentMarkdown without model field omits model from frontmatter entirely (model agnosticism per D-03/PLGN-04)
    - generateAgentMarkdown includes placeholder prompt section with guidance comments
    - generateSkillMarkdown({ name: "my-skill", description: "Does things" }) produces markdown with "name: my-skill" and sections "## What I do", "## Rules", "## Examples"
    - generateCommandMarkdown({ name: "review", description: "Reviews code" }) produces markdown with description in frontmatter and $ARGUMENTS placeholder in body
    - generateCommandMarkdown with optional agent/model fields includes them in frontmatter
    - All templates use yaml.stringify() for frontmatter generation (not string concatenation)
  </behavior>
  <action>
    Create `src/templates/` directory with three template modules. Each exports a pure function that takes a readonly input interface and returns a string. All use `import { stringify } from "yaml"` for frontmatter.

    1. `src/templates/agent-template.ts`:
       - Export `AgentTemplateInput` interface (readonly fields):
         - `name: string` (required)
         - `description: string` (required)
         - `mode: "primary" | "subagent" | "all"` (required, default applied by tool schema)
         - `model?: string` (optional — omit from frontmatter if not provided, per model agnosticism)
         - `temperature?: number` (optional)
         - `permission?: Record<string, string>` (optional, defaults to strict read-only)
       - Export `generateAgentMarkdown(input: AgentTemplateInput): string`
       - Default permission object: `{ read: "allow", edit: "deny", bash: "deny", webfetch: "deny", task: "deny" }` (per D-01)
       - Build frontmatter object: always include description, mode, permission. Conditionally include model, temperature only if provided.
       - Use `stringify(frontmatter).trim()` for YAML block
       - Body includes: "You are {name}, an AI agent." followed by TODO comment, ## Role, ## Instructions sections (per D-03)

    2. `src/templates/skill-template.ts`:
       - Export `SkillTemplateInput` interface (readonly fields):
         - `name: string` (required)
         - `description: string` (required)
         - `license?: string` (optional)
         - `compatibility?: string` (optional)
       - Export `generateSkillMarkdown(input: SkillTemplateInput): string`
       - Frontmatter: name, description (required), license, compatibility (conditional)
       - Body sections (per D-06): `## What I do`, `## Rules`, `## Examples` with placeholder content

    3. `src/templates/command-template.ts`:
       - Export `CommandTemplateInput` interface (readonly fields):
         - `name: string` (required — used in body text, not in frontmatter)
         - `description: string` (required)
         - `agent?: string` (optional)
         - `model?: string` (optional)
       - Export `generateCommandMarkdown(input: CommandTemplateInput): string`
       - Frontmatter: description (required), agent, model (conditional)
       - Body: includes `$ARGUMENTS` placeholder and usage guidance text (per D-08)

    4. Create `tests/templates.test.ts` with tests for all behavior cases. Write tests FIRST (RED), then implement (GREEN). Test that:
       - Output starts with `---\n` and contains closing `---\n`
       - YAML frontmatter contains expected fields
       - Optional fields are omitted when not provided
       - Body sections contain expected headings
       - Special characters in descriptions are handled correctly (colon, quotes)
  </action>
  <verify>
    <automated>bun test tests/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "generateAgentMarkdown" src/templates/agent-template.ts
    - grep -q "generateSkillMarkdown" src/templates/skill-template.ts
    - grep -q "generateCommandMarkdown" src/templates/command-template.ts
    - grep -q 'stringify' src/templates/agent-template.ts
    - grep -q 'stringify' src/templates/skill-template.ts
    - grep -q 'stringify' src/templates/command-template.ts
    - grep -q "AgentTemplateInput" src/templates/agent-template.ts
    - grep -q "SkillTemplateInput" src/templates/skill-template.ts
    - grep -q "CommandTemplateInput" src/templates/command-template.ts
    - bun test tests/templates.test.ts passes with 0 failures
  </acceptance_criteria>
  <done>
    - Three template modules produce valid markdown with YAML frontmatter via yaml.stringify()
    - Agent template defaults to subagent mode with strict read-only permissions
    - Agent template omits model field when not provided (model agnosticism)
    - Skill template includes all required sections (What I do, Rules, Examples)
    - Command template includes $ARGUMENTS placeholder
    - All template functions are pure (no side effects, no filesystem access)
    - Tests cover required fields, optional fields, special characters
  </done>
</task>

</tasks>

<verification>
- `bun test tests/validators.test.ts tests/templates.test.ts` — all tests pass
- `bun lint` — no linting errors in new files
- `grep -r "stringify" src/templates/` confirms yaml package usage in all templates
- `grep -r "validateAssetName\|validateCommandName" src/utils/validators.ts` confirms exports
</verification>

<success_criteria>
- Validators reject invalid names and accept valid ones per OpenCode's naming rules
- Templates produce correctly-formatted markdown with YAML frontmatter for all three asset types
- yaml package installed as runtime dependency
- All tests pass, all functions are pure and side-effect-free
- Ready for Plan 02 to import and use in tool implementations
</success_criteria>

<output>
After completion, create `.planning/phases/02-creation-tooling/02-01-SUMMARY.md`
</output>
