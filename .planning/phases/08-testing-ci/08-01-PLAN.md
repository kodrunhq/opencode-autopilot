---
phase: 08-testing-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/documenter.ts
  - src/agents/metaprompter.ts
  - src/agents/orchestrator.ts
  - src/agents/pr-reviewer.ts
  - src/agents/researcher.ts
  - src/agents/pipeline/oc-architect.ts
  - src/agents/pipeline/oc-challenger.ts
  - src/agents/pipeline/oc-critic.ts
  - src/agents/pipeline/oc-explorer.ts
  - src/agents/pipeline/oc-implementer.ts
  - src/agents/pipeline/oc-planner.ts
  - src/agents/pipeline/oc-researcher.ts
  - src/agents/pipeline/oc-retrospector.ts
  - src/agents/pipeline/oc-reviewer.ts
  - src/agents/pipeline/oc-shipper.ts
  - src/orchestrator/handlers/build.ts
  - src/review/memory.ts
  - src/tools/orchestrate.ts
  - src/tools/review.ts
  - tests/handlers-early.test.ts
  - bunfig.toml
  - tests/index.test.ts
autonomous: true
requirements:
  - TCID-01
  - TCID-02
  - TCID-03

must_haves:
  truths:
    - "bunx tsc --noEmit exits with code 0 (zero type errors)"
    - "bun test --coverage passes with coverage thresholds enforced"
    - "Plugin entry point test verifies all 11 oc_* tool names are registered"
  artifacts:
    - path: "bunfig.toml"
      provides: "Coverage threshold configuration"
      contains: "coverageThreshold"
    - path: "tests/index.test.ts"
      provides: "Tool registration smoke test"
      contains: "oc_orchestrate"
  key_links:
    - from: "bunfig.toml"
      to: "bun test --coverage"
      via: "coverageThreshold enforcement"
      pattern: "coverageThreshold.*lines.*0\\.90"
---

<objective>
Fix all TypeScript type errors across 19 source files, add coverage thresholds via bunfig.toml, and add a tool registration smoke test.

Purpose: Establish a type-clean codebase and coverage floor so CI (Plan 02) can enforce them as gates.
Output: Zero tsc errors, bunfig.toml with 90% coverage floor, comprehensive plugin smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-testing-ci/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all TypeScript type errors across the codebase</name>
  <files>
    src/agents/documenter.ts
    src/agents/metaprompter.ts
    src/agents/orchestrator.ts
    src/agents/pr-reviewer.ts
    src/agents/researcher.ts
    src/agents/pipeline/oc-architect.ts
    src/agents/pipeline/oc-challenger.ts
    src/agents/pipeline/oc-critic.ts
    src/agents/pipeline/oc-explorer.ts
    src/agents/pipeline/oc-implementer.ts
    src/agents/pipeline/oc-planner.ts
    src/agents/pipeline/oc-researcher.ts
    src/agents/pipeline/oc-retrospector.ts
    src/agents/pipeline/oc-reviewer.ts
    src/agents/pipeline/oc-shipper.ts
    src/orchestrator/handlers/build.ts
    src/review/memory.ts
    src/tools/orchestrate.ts
    src/tools/review.ts
    tests/handlers-early.test.ts
  </files>
  <read_first>
    src/agents/documenter.ts
    src/agents/metaprompter.ts
    src/agents/orchestrator.ts
    src/agents/pr-reviewer.ts
    src/agents/researcher.ts
    src/agents/pipeline/oc-architect.ts
    src/agents/pipeline/oc-challenger.ts
    src/agents/pipeline/oc-critic.ts
    src/agents/pipeline/oc-explorer.ts
    src/agents/pipeline/oc-implementer.ts
    src/agents/pipeline/oc-planner.ts
    src/agents/pipeline/oc-researcher.ts
    src/agents/pipeline/oc-retrospector.ts
    src/agents/pipeline/oc-reviewer.ts
    src/agents/pipeline/oc-shipper.ts
    src/orchestrator/handlers/build.ts
    src/review/memory.ts
    src/tools/orchestrate.ts
    src/tools/review.ts
    tests/handlers-early.test.ts
  </read_first>
  <action>
Fix 21 TypeScript errors across 19 files. Run `bunx tsc --noEmit` first to confirm error locations, then fix each category:

**Category 1: Agent permission literal narrowing (15 agent files)**
Files: all agent files listed above (documenter, metaprompter, orchestrator, pr-reviewer, researcher, and all 11 pipeline/*.ts files).
Error: `Object.freeze()` widens `"allow"` / `"deny"` to `string`, incompatible with `AgentConfig.permission.edit` which expects `"allow" | "deny" | "ask" | undefined`.
Fix: Add `as const` to the `permission` object literal inside each `Object.freeze()` call. Example for documenter.ts:
```typescript
export const documenterAgent: Readonly<AgentConfig> = Object.freeze({
  // ... other fields unchanged ...
  permission: {
    edit: "allow",
    bash: "deny",
    webfetch: "deny",
  } as const,
});
```
Apply the same `as const` pattern to the `permission` property in ALL 15 agent files. Do NOT change any other part of the agent definitions.

**Category 2: Readonly array to mutable array assignment (build.ts — 5 errors, memory.ts — 1 error)**

File `src/orchestrator/handlers/build.ts`: Lines 206, 230, 251, 270, 353 assign `readonly` task arrays to mutable `tasks` property. The fix is to spread the readonly array into a new mutable array at the assignment site. For each error location, change the assignment from `tasks: readonlyArray` to `tasks: [...readonlyArray]`. If the tasks array is being constructed via `.map()` on a readonly source, the result is already mutable — in that case the issue may be the type annotation. Read the code carefully at each error line to determine the minimal fix.

File `src/review/memory.ts` line 109: Same pattern — a readonly findings array is assigned to a mutable array property. Fix by spreading: `recentFindings: [...readonlyFindings]` (or equivalent minimal change).

**Category 3: Type assertion issues (2 files)**

File `src/tools/orchestrate.ts` line 190: `as Record<string, unknown>` cast on `DispatchResult` fails because DispatchResult has no index signature. Fix by casting through `unknown`: `as unknown as Record<string, unknown>`.

File `src/tools/review.ts` line 113: `as { readonly name: string; readonly prompt: string; }` cast on `SelectableAgent` fails because `SelectableAgent` lacks `prompt`. Read the surrounding code to understand what's needed — likely the cast target type should be adjusted to match `SelectableAgent`'s actual shape, or the cast should go through `unknown`.

**Category 4: Test type error (1 file)**

File `tests/handlers-early.test.ts` line 134: Object literal has extra `signal` property not in the confidence entry type. Remove the `signal` property from the test object literal, or if `signal` is needed for the test's intent, restructure to avoid the excess property error.

**After all fixes:** Run `bunx tsc --noEmit` and verify exit code 0 with zero errors.
**Then:** Run `bun test` and verify all 530+ tests still pass (no behavioral regressions).
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits with code 0
    - `bun test` exits with code 0 (all existing tests pass)
    - No agent file contains `permission: {` without `as const` on the permission object
    - src/orchestrator/handlers/build.ts contains no TS4104 readonly-to-mutable errors
    - src/review/memory.ts contains no TS2322 readonly array assignment errors
    - src/tools/orchestrate.ts contains `as unknown as Record<string, unknown>` (or equivalent double-cast)
    - tests/handlers-early.test.ts does not have a `signal` property in a confidence entry object literal
  </acceptance_criteria>
  <done>Zero TypeScript errors. All existing tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add bunfig.toml coverage thresholds and tool registration smoke test</name>
  <files>
    bunfig.toml
    tests/index.test.ts
  </files>
  <read_first>
    tests/index.test.ts
    src/index.ts
    package.json
  </read_first>
  <action>
**Part A: Create bunfig.toml**

Create `bunfig.toml` in the project root with:
```toml
[test]
coverageThreshold = { lines = 0.90, functions = 0.90 }
coverageSkipTestFiles = true
```

This sets a 90% floor for both line and function coverage. The project currently has 97%+ coverage, so 90% provides regression protection while leaving headroom for new WIP code.

**Part B: Add tool registration smoke test**

Add a new test to `tests/index.test.ts` inside the existing `describe("plugin entry point", ...)` block. Add it after the existing tests. The test must verify that all 11 expected `oc_*` tool names are registered:

```typescript
test("registers all expected tools", async () => {
  const result = await plugin(mockInput);
  const toolNames = Object.keys(result.tool ?? {});
  const expected = [
    "oc_placeholder",
    "oc_create_agent",
    "oc_create_skill",
    "oc_create_command",
    "oc_state",
    "oc_confidence",
    "oc_phase",
    "oc_plan",
    "oc_orchestrate",
    "oc_forensics",
    "oc_review",
  ];
  expect(toolNames.sort()).toEqual(expected.sort());
});
```

This catches registration regressions — if a tool is added to `src/index.ts` but not listed here (or vice versa), the test fails.

**After both changes:** Run `bun test --coverage` and verify coverage thresholds are enforced and all tests pass.
  </action>
  <verify>
    <automated>bun test --coverage 2>&1 | tail -20; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - File `bunfig.toml` exists in project root
    - `bunfig.toml` contains `coverageThreshold = { lines = 0.90, functions = 0.90 }`
    - `bunfig.toml` contains `coverageSkipTestFiles = true`
    - `tests/index.test.ts` contains `"registers all expected tools"`
    - `tests/index.test.ts` contains `"oc_orchestrate"` in the expected array
    - `tests/index.test.ts` contains `"oc_forensics"` in the expected array
    - `bun test --coverage` exits with code 0
    - Test output shows the new "registers all expected tools" test passing
  </acceptance_criteria>
  <done>bunfig.toml enforces 90% coverage floor. Smoke test verifies all 11 tool registrations. Both pass.</done>
</task>

</tasks>

<verification>
1. `bunx tsc --noEmit` exits with code 0 — zero type errors
2. `bun test --coverage` exits with code 0 — all tests pass, coverage above 90% threshold
3. New smoke test validates all 11 oc_* tool names
</verification>

<success_criteria>
- Zero TypeScript errors across the entire codebase
- Coverage thresholds enforced at 90% for lines and functions
- All 11 tool registrations verified by smoke test
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing-ci/08-01-SUMMARY.md`
</output>
