---
phase: 07-learning-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/lesson-schemas.ts
  - src/orchestrator/lesson-types.ts
  - src/orchestrator/lesson-memory.ts
  - tests/orchestrator/lesson-memory.test.ts
autonomous: true
requirements: [LRNR-01]
must_haves:
  truths:
    - "Lessons persist to .opencode-assets/lesson-memory.json with atomic writes"
    - "Stale lessons (>90 days) are pruned on load"
    - "Lesson store is capped at 50 entries, keeping newest"
    - "Invalid/missing lesson files return null gracefully"
  artifacts:
    - path: "src/orchestrator/lesson-schemas.ts"
      provides: "Zod schemas for lessons and lesson memory"
      exports: ["LESSON_DOMAINS", "lessonDomainSchema", "lessonSchema", "lessonMemorySchema"]
    - path: "src/orchestrator/lesson-types.ts"
      provides: "TypeScript types inferred from schemas"
      exports: ["Lesson", "LessonDomain", "LessonMemory"]
    - path: "src/orchestrator/lesson-memory.ts"
      provides: "Load/save/prune lesson memory with atomic writes"
      exports: ["loadLessonMemory", "saveLessonMemory", "pruneLessons", "createEmptyLessonMemory"]
    - path: "tests/orchestrator/lesson-memory.test.ts"
      provides: "Unit tests for lesson memory module"
  key_links:
    - from: "src/orchestrator/lesson-memory.ts"
      to: "src/orchestrator/lesson-schemas.ts"
      via: "import lessonMemorySchema"
      pattern: "lessonMemorySchema\\.parse"
    - from: "src/orchestrator/lesson-memory.ts"
      to: ".opencode-assets/lesson-memory.json"
      via: "readFile/writeFile with atomic rename"
      pattern: "rename\\(tmpPath"
---

<objective>
Create the lesson memory data layer: Zod schemas, TypeScript types, and a load/save/prune module that mirrors the review/memory.ts pattern with 90-day TTL and 50-lesson cap.

Purpose: Foundation for institutional memory -- all other phase 7 work depends on these schemas and persistence functions.
Output: Three source files (schemas, types, memory module) and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-learning-resilience/07-CONTEXT.md
@.planning/phases/07-learning-resilience/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/review/schemas.ts (pattern reference):
```typescript
export const reviewMemorySchema = z.object({
  schemaVersion: z.literal(1),
  projectProfile: z.object({...}),
  recentFindings: z.array(reviewFindingSchema).max(100),
  falsePositives: z.array(falsePositiveSchema).max(50),
  lastReviewedAt: z.string().max(128).nullable(),
});
```

From src/review/memory.ts (pattern reference):
```typescript
export function createEmptyMemory(): ReviewMemory
export async function loadReviewMemory(projectRoot: string): Promise<ReviewMemory | null>
export async function saveReviewMemory(memory: ReviewMemory, projectRoot: string): Promise<void>
export function pruneMemory(memory: ReviewMemory): ReviewMemory
```

From src/utils/fs-helpers.ts:
```typescript
export async function ensureDir(dirPath: string): Promise<void>
export function isEnoentError(error: unknown): boolean
```

From src/orchestrator/schemas.ts:
```typescript
export const phaseSchema = z.enum(PHASES);
// PHASES = ["RECON","CHALLENGE","ARCHITECT","EXPLORE","PLAN","BUILD","SHIP","RETROSPECTIVE"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lesson schemas, types, and memory module</name>
  <files>src/orchestrator/lesson-schemas.ts, src/orchestrator/lesson-types.ts, src/orchestrator/lesson-memory.ts, tests/orchestrator/lesson-memory.test.ts</files>
  <read_first>src/review/schemas.ts, src/review/memory.ts, src/review/types.ts, src/utils/fs-helpers.ts</read_first>
  <behavior>
    - loadLessonMemory returns null when file does not exist
    - loadLessonMemory returns null on malformed JSON (SyntaxError)
    - loadLessonMemory returns null on Zod validation failure
    - loadLessonMemory prunes stale lessons (>90 days) on load
    - loadLessonMemory caps at 50 lessons (keeps newest by extractedAt)
    - saveLessonMemory writes atomically (tmp + rename)
    - saveLessonMemory validates through schema before writing
    - pruneLessons removes entries older than 90 days
    - pruneLessons keeps only newest 50 when over cap
    - pruneLessons returns a frozen object (never mutates input)
    - createEmptyLessonMemory returns valid schema-conforming empty object
    - lessonSchema validates content, domain, extractedAt, sourcePhase fields
    - lessonDomainSchema only accepts "architecture", "testing", "review", "planning"
    - lessonMemorySchema has schemaVersion: 1, lessons array, lastUpdatedAt
  </behavior>
  <action>
    Create three source files following the exact review/memory.ts pattern (per D-01):

    **src/orchestrator/lesson-schemas.ts:**
    - Export `LESSON_DOMAINS = Object.freeze(["architecture", "testing", "review", "planning"] as const)` (per D-02)
    - Export `lessonDomainSchema = z.enum(LESSON_DOMAINS)`
    - Export `lessonSchema = z.object({ content: z.string().max(1024), domain: lessonDomainSchema, extractedAt: z.string().max(128), sourcePhase: z.string().max(128) })`
    - Export `lessonMemorySchema = z.object({ schemaVersion: z.literal(1), lessons: z.array(lessonSchema).max(50), lastUpdatedAt: z.string().max(128).nullable() })`
    - Import z from "zod"

    **src/orchestrator/lesson-types.ts:**
    - Export `type Lesson = z.infer<typeof lessonSchema>`
    - Export `type LessonDomain = z.infer<typeof lessonDomainSchema>`
    - Export `type LessonMemory = z.infer<typeof lessonMemorySchema>`
    - Pattern: same as src/orchestrator/types.ts

    **src/orchestrator/lesson-memory.ts:**
    - Constants: `LESSON_FILE = "lesson-memory.json"`, `MAX_LESSONS = 50`, `NINETY_DAYS_MS = 90 * 24 * 60 * 60 * 1000` (per D-10, D-11)
    - `loadLessonMemory(projectRoot: string): Promise<LessonMemory | null>` -- read from `join(projectRoot, ".opencode-assets", LESSON_FILE)`, parse JSON, validate via `lessonMemorySchema.parse()`, prune on load, return null on ENOENT/SyntaxError/ZodError
    - `saveLessonMemory(memory: LessonMemory, projectRoot: string): Promise<void>` -- validate via schema.parse, ensureDir, write to tmp file, rename atomically
    - `pruneLessons(memory: LessonMemory): LessonMemory` -- filter out entries where `Date.now() - new Date(extractedAt).getTime() >= NINETY_DAYS_MS`, sort remaining by extractedAt descending, take first MAX_LESSONS, return `Object.freeze({...})` (never mutate)
    - `createEmptyLessonMemory(): LessonMemory` -- return `lessonMemorySchema.parse({ schemaVersion: 1, lessons: [], lastUpdatedAt: null })`
    - Imports: `readFile, rename, writeFile` from "node:fs/promises", `join` from "node:path", `ensureDir, isEnoentError` from "../utils/fs-helpers", schemas from "./lesson-schemas", types from "./lesson-types"

    **tests/orchestrator/lesson-memory.test.ts:**
    - Use `bun:test` (describe/test/expect/beforeEach/afterEach)
    - Create temp dir per test, clean up after
    - Test all behaviors listed above
    - For TTL tests: create lessons with extractedAt set to 91 days ago
    - For cap tests: create 55 lessons and verify only 50 remain after prune
  </action>
  <verify>
    <automated>bun test tests/orchestrator/lesson-memory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "LESSON_DOMAINS" src/orchestrator/lesson-schemas.ts
    - grep -q "architecture.*testing.*review.*planning" src/orchestrator/lesson-schemas.ts
    - grep -q "lessonMemorySchema" src/orchestrator/lesson-schemas.ts
    - grep -q "type Lesson" src/orchestrator/lesson-types.ts
    - grep -q "type LessonMemory" src/orchestrator/lesson-types.ts
    - grep -q "loadLessonMemory" src/orchestrator/lesson-memory.ts
    - grep -q "saveLessonMemory" src/orchestrator/lesson-memory.ts
    - grep -q "pruneLessons" src/orchestrator/lesson-memory.ts
    - grep -q "createEmptyLessonMemory" src/orchestrator/lesson-memory.ts
    - grep -q "NINETY_DAYS_MS" src/orchestrator/lesson-memory.ts
    - grep -q "MAX_LESSONS.*50" src/orchestrator/lesson-memory.ts
    - grep -q "Object.freeze" src/orchestrator/lesson-memory.ts
    - grep -q "rename(tmpPath" src/orchestrator/lesson-memory.ts
    - bun test tests/orchestrator/lesson-memory.test.ts passes
  </acceptance_criteria>
  <done>Lesson schemas define 4 domains, lesson memory module loads/saves/prunes with 90-day TTL and 50-lesson cap, all tests pass</done>
</task>

</tasks>

<verification>
- `bun test tests/orchestrator/lesson-memory.test.ts` passes all cases
- `bun run lint` passes on new files
- Lesson schemas are importable from lesson-schemas.ts
- Memory functions are importable from lesson-memory.ts
</verification>

<success_criteria>
- Lesson memory module mirrors review/memory.ts pattern exactly
- 90-day TTL prunes stale entries on load
- 50-lesson cap enforced via pruneLessons
- Atomic writes via tmp + rename
- All returns are Object.freeze'd (immutable)
- Zod validation on both load and save (bidirectional)
</success_criteria>

<output>
After completion, create `.planning/phases/07-learning-resilience/07-01-SUMMARY.md`
</output>
