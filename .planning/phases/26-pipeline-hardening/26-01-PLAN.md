# Phase 26-01: Pipeline Hardening & Orchestration Fixes

**Goal:** The orchestration pipeline runs a full RECON-to-RETROSPECTIVE cycle on any project without infinite loops, path mismatches, context bloat, or silent failures, with real-time logging and user-visible progress throughout.

**Sources:** Live failure analysis (Gloomberg session `ses_2aa4a650bffejaeA3Bwk3IRefw`), Claude Code codebase exploration, Claude Opus Desktop full review (22 issues).

**Baseline:** 1341 tests passing, 0 failures. All changes must maintain this.

---

## Wave 1 — Stop the Bleeding (P0 + Safety Valves)

No dependencies between tasks. All touch different files.

### Task 1: Fix artifact path mismatch (the root cause of the infinite loop)

**Issues:** Opus #1, Opus #22, Opus #19 (partial)
**Priority:** P0
**Files:**
- `src/orchestrator/artifacts.ts` — change `getArtifactRef` to accept `artifactDir` and return absolute paths
- `src/orchestrator/handlers/recon.ts` — use single canonical path (remove duplicate `Output:` line)
- `src/orchestrator/handlers/challenge.ts` — use absolute path from updated `getArtifactRef`
- `src/orchestrator/handlers/architect.ts` — use absolute path from updated `getArtifactRef`
- `src/orchestrator/handlers/plan.ts` — use absolute path
- `src/orchestrator/handlers/ship.ts` — use absolute path
- `src/orchestrator/handlers/build.ts` — update artifact refs
- `src/orchestrator/handlers/retrospective.ts` — update artifact refs

**Action:**
1. Change `getArtifactRef` signature to `(artifactDir: string, phase: Phase, filename: string) => string` returning `join(getPhaseDir(artifactDir, phase), filename)`.
2. Update all handler call sites to pass `artifactDir`.
3. Remove the duplicate relative+absolute path pattern in RECON handler.
4. Ensure ARCHITECT handler's `fileExists` check and prompt path now point to the same location.

**Verification:** `bun test` — all handler tests + artifact tests pass. Add new test: "ARCHITECT prompt path matches fileExists check path".

**Done:** Every handler prompt references the same absolute path that the handler checks for idempotency.

---

### Task 2: Add per-phase circuit breaker (max dispatch counter)

**Issues:** Opus #20, our original Task 2
**Priority:** P0
**Files:**
- `src/orchestrator/schemas.ts` — add `phaseDispatchCounts` to PipelineState schema
- `src/orchestrator/types.ts` — add field to type
- `src/tools/orchestrate.ts` — increment counter before each dispatch, check limit

**Action:**
1. Add `phaseDispatchCounts: z.record(z.string(), z.number()).default({})` to schema.
2. In `processHandlerResult`, before returning any "dispatch" action, increment `state.phaseDispatchCounts[phase]`.
3. If count exceeds limit, return `{ action: "error", message: "Phase ${phase} exceeded max dispatches" }`.
4. Limits: `{ RECON: 3, CHALLENGE: 3, ARCHITECT: 10, EXPLORE: 3, PLAN: 5, BUILD: 100, SHIP: 5, RETROSPECTIVE: 3 }`.

**Verification:** `bun test tests/orchestrate-pipeline.test.ts` — add test: "returns error when phase exceeds max dispatches".

**Done:** No phase can dispatch infinitely. ARCHITECT gets 10 (arena may need multiple proposals + critic).

---

### Task 3: Fix empty confidence ledger default (arena depth)

**Issues:** Opus #2
**Priority:** P0
**Files:**
- `src/orchestrator/confidence.ts` — change default from `"MEDIUM"` to `"HIGH"`

**Action:** Line ~40: change `let dominant: ConfidenceLevel = "MEDIUM"` to `"HIGH"`. Rationale: no evidence of low confidence should mean depth=1 (fast path), not depth=2 (arena).

**Verification:** `bun test tests/orchestrator/confidence.test.ts` — update test for empty ledger.

**Done:** Empty confidence ledger produces depth=1 instead of depth=2.

---

### Task 4: Fix temp file collision risk in state saves

**Issues:** Opus #6
**Priority:** P1
**Files:**
- `src/orchestrator/state.ts` — use `randomBytes(8).toString("hex")` instead of `Date.now()`
- `src/orchestrator/lesson-memory.ts` — same fix

**Action:** Import `randomBytes` from `node:crypto`. Replace `Date.now()` suffix in both files.

**Verification:** `bun test tests/orchestrator/state.test.ts`

**Done:** Temp file suffixes are crypto-random, matching `saveConfig` pattern.

---

## Wave 2 — Fix Context & Observability (depends on Wave 1 path fixes)

### Task 5: Reduce skill injection to phase-relevant summaries

**Issues:** Opus #8, #9, #21, our original Task 4
**Priority:** P1
**Files:**
- `src/skills/adaptive-injector.ts` — add phase-aware filtering, summary mode, deterministic ordering
- `src/skills/loader.ts` — sort skills alphabetically after loading
- `src/tools/orchestrate.ts` — pass phase name and reduced budget to injector

**Action:**
1. In `loader.ts`: sort loaded skills by name before returning the Map.
2. In `adaptive-injector.ts`:
   - Add `PHASE_SKILL_MAP`: `{ RECON: ["plan-writing"], CHALLENGE: ["plan-writing"], ARCHITECT: ["plan-writing"], PLAN: ["plan-writing", "plan-executing"], BUILD: ["coding-standards", "tdd-workflow"], SHIP: ["plan-executing"], RETROSPECTIVE: [] }`.
   - Add `buildSkillSummary(skill)` that returns frontmatter name + description (max 200 chars). No full content.
   - Remove the newline-collapsing `replace(/[\r\n]+/g, " ")` — if full content is ever needed, preserve structure and truncate at `##` boundaries.
   - Add `mode: "summary" | "full"` parameter to `buildMultiSkillContext`. Default "summary" for orchestration.
3. In `orchestrate.ts`: pass `{ phase, budget: 1500, mode: "summary" }` to `injectSkillContext`.

**Verification:** `bun test tests/skills/adaptive-injector.test.ts` — add tests for deterministic ordering, summary mode, phase filtering.

**Done:** Orchestration dispatches get ~500 chars of skill context instead of ~32KB. Skills are deterministic across calls.

---

### Task 6: Add numbered phase folders + migration

**Issues:** Our original Task 8
**Priority:** P2
**Files:**
- `src/orchestrator/artifacts.ts` — add `PHASE_NUMBERS` map, update `getPhaseDir`
- `src/orchestrator/state.ts` — add migration on `loadState` to rename old folders

**Action:**
1. Add `PHASE_NUMBERS: Record<Phase, string>` = `{ RECON: "01", CHALLENGE: "02", ARCHITECT: "03", EXPLORE: "04", PLAN: "05", BUILD: "06", SHIP: "07", RETROSPECTIVE: "08" }`.
2. Change `getPhaseDir` to return `phases/${number}-${phase}/` (e.g., `phases/01-RECON/`).
3. In `loadState`, if unnumbered folders exist, rename them.

**Verification:** `bun test` — all tests updated for new path format.

**Done:** Phase folders have numeric prefixes for user clarity.

---

### Task 7: Add real-time orchestration logging

**Issues:** Our original Task 5
**Priority:** P1
**Files:**
- New file: `src/orchestrator/orchestration-logger.ts`
- `src/tools/orchestrate.ts` — call logger after each handler result

**Action:**
1. Create `orchestration-logger.ts` with `logOrchestrationEvent(artifactDir, event)` — appends JSONL to `.opencode-autopilot/orchestration.jsonl`.
2. Event types: `phase_dispatch`, `phase_complete`, `phase_error`, `loop_detected`.
3. Each event: `{ timestamp, phase, action, agent, promptLength, attempt }`.
4. Call in `processHandlerResult` after every dispatch/complete/error.
5. Append-only, sync write (survives crashes).

**Verification:** `bun test tests/orchestrate-pipeline.test.ts` — verify JSONL entries exist after pipeline run.

**Done:** Every orchestration action is logged to a project-local file that persists even if the session crashes.

---

### Task 8: Add user-visible progress reporting

**Issues:** Our original Task 7
**Priority:** P2
**Files:**
- `src/tools/orchestrate.ts` — add `_userProgress` field to dispatch results
- `src/orchestrator/phase.ts` — add `PHASE_INDEX` map for numbering

**Action:**
1. Add `PHASE_INDEX: Record<Phase, number>` = `{ RECON: 1, ..., RETROSPECTIVE: 8 }`.
2. In every dispatch result, add `_userProgress: "Phase ${index}/8: ${phase} — ${progress} (attempt ${count})"`.
3. In every complete result, add toast-style message: `"Completed ${phase} (${index}/8), advancing to ${nextPhase}"`.

**Verification:** Check progress strings in test output.

**Done:** Every tool response tells the user which phase they're in, what's happening, and which attempt it is.

---

## Wave 3 — Memory, Hooks & Code Quality (depends on Wave 2 logging)

### Task 9: Wire orchestration events to memory capture

**Issues:** Our original Task 6
**Priority:** P2
**Files:**
- `src/tools/orchestrate.ts` — emit decision events to event store
- `src/index.ts` — pass event store reference to orchestrate tool

**Action:**
1. Add `emitOrchestrationDecision(eventStore, phase, decision, confidence)` helper.
2. Call on every phase completion and every dispatch.
3. Memory capture handler already listens for `app.decision` — just need to fire them.

**Verification:** `bun test tests/orchestrate-pipeline.test.ts` — verify memory capture receives events.

**Done:** Orchestration decisions are persisted in memory and available for future session context.

---

### Task 10: Fix memory access-count tracking (dead code)

**Issues:** Opus #3
**Priority:** P1
**Files:**
- `src/memory/retrieval.ts` — call `updateAccessCount` for retrieved observations

**Action:** In `retrieveMemoryContext`, after selecting observations for injection, call `updateAccessCount(obs.id, db)` for each.

**Verification:** `bun test tests/memory/` — add test: "access count increments when observation is retrieved".

**Done:** Decay model's access-count boost actually works.

---

### Task 11: Fix anti-slop performance (debounce file reads)

**Issues:** Opus #4
**Priority:** P1
**Files:**
- `src/hooks/anti-slop.ts` — add path-based debounce cache

**Action:** Track `Map<filePath, lastScanTimestamp>`. Skip re-scan if path was scanned within 5 seconds.

**Verification:** `bun test tests/hooks/anti-slop.test.ts`

**Done:** Anti-slop hook doesn't re-read the same file on rapid successive writes.

---

### Task 12: Tighten anti-slop false positives

**Issues:** Opus #5
**Priority:** P1
**Files:**
- `src/hooks/slop-patterns.ts` — tighten patterns, require 2+ indicators per file

**Action:**
1. Change broad patterns (`robust`, `comprehensive`, `powerful`) to require narrating context: `/^(?:this|the|it)\s.*\brobust\b/i`.
2. Add threshold: only warn if 2+ distinct slop patterns match in the same file.
3. Exclude `#!` shebangs and hex color codes from `#` comment pattern.

**Verification:** `bun test tests/hooks/slop-patterns.test.ts`

**Done:** Legitimate technical terms don't trigger false warnings.

---

### Task 13: Fix deprecated asset cleanup ownership check

**Issues:** Opus #7
**Priority:** P1
**Files:**
- `src/installer.ts` — add content marker check before deleting

**Action:** Before deleting a deprecated asset, read first line. Only delete if it contains `<!-- opencode-autopilot-managed -->` or was created within 1 second of plugin load (installer-created).

**Verification:** `bun test tests/installer.test.ts` — add test: "does not delete user file with same name as deprecated asset".

**Done:** User-created files with names matching deprecated assets are preserved.

---

### Task 14: Fix setTimeout pruning in memory capture

**Issues:** Opus #14
**Priority:** P2
**Files:**
- `src/memory/capture.ts` — replace `setTimeout(fn, 0)` with `queueMicrotask(fn)`

**Action:** One-line change. `queueMicrotask` runs before the event loop clears, guaranteeing execution even if the process is exiting.

**Verification:** `bun test tests/memory/capture.test.ts`

**Done:** Observation pruning is guaranteed to execute.

---

### Task 15: Add FTS5 index optimization

**Issues:** Opus #13
**Priority:** P2
**Files:**
- `src/memory/decay.ts` — add `OPTIMIZE` call in `pruneStaleObservations`

**Action:** After pruning, execute `INSERT INTO observations_fts(observations_fts) VALUES('optimize')`.

**Verification:** `bun test tests/memory/decay.test.ts`

**Done:** FTS5 index stays compact over time.

---

### Task 16: Remove dead code (computeDiffRelevance)

**Issues:** Opus #15
**Priority:** P2
**Files:**
- `src/review/selection.ts` — remove `computeDiffRelevance` call and function

**Action:** Delete the unused call and the function. If relevance-based ordering is wanted later, it should be re-implemented with actual agent-type metadata instead of hardcoded name checks.

**Verification:** `bun test tests/review/`

**Done:** No dead code confusing future contributors.

---

### Task 17: Standardize handler completion logic

**Issues:** Opus #18, #19
**Priority:** P3
**Files:**
- `src/orchestrator/handlers/recon.ts` — add artifact existence check
- `src/orchestrator/handlers/challenge.ts` — add artifact existence check

**Action:** After checking `if (result)`, also verify the expected artifact file exists at the absolute path. If result is truthy but file doesn't exist, log a warning but still complete (the result string itself is the output). This makes all handlers use a consistent "check result + verify artifact" pattern.

**Verification:** `bun test tests/handlers-early.test.ts`

**Done:** All early-phase handlers follow the same completion strategy.

---

## Deferred (Out of Scope for Phase 26)

These items from the Opus report are real but lower priority. Track as backlog:

| Opus # | Issue | Why Deferred |
|--------|-------|-------------|
| #10 | handleBuild 400-line refactor | Major refactor, needs its own phase |
| #11 | Memory DB singleton | Works fine for serial execution, only matters for parallel tests |
| #12 | extractJson parser fragility | Edge case, current parser handles 95% of cases |
| #16 | Memory capture rate limiting | Latency is acceptable for current pipeline sizes |
| #17 | Template sanitization scope | Mustache-only is sufficient for current codebase |

---

## Verification Plan

After all waves:

1. `bun test` — all 1341+ tests pass (plus new tests from each task)
2. `bun run lint` — zero errors
3. Manual smoke test: run `oc_orchestrate({ idea: "Build a simple REST API" })` on a test project and verify:
   - Pipeline advances through phases without looping
   - `.opencode-autopilot/orchestration.jsonl` contains entries for each dispatch
   - Phase folders are numbered (`01-RECON/`, `02-CHALLENGE/`, etc.)
   - Tool output includes `_userProgress` with phase numbers
   - No 32KB skill context in dispatch prompts
   - Memory DB has orchestration decision entries

---

## Issue Cross-Reference

| Task | Our Analysis | Opus Report | Priority |
|------|-------------|-------------|----------|
| 1 | Task 1 + Task 3 | #1, #22, #19 | P0 |
| 2 | Task 2 | #20 | P0 |
| 3 | — | #2 | P0 |
| 4 | — | #6 | P1 |
| 5 | Task 4 | #8, #9, #21 | P1 |
| 6 | Task 8 | — | P2 |
| 7 | Task 5 | — | P1 |
| 8 | Task 7 | — | P2 |
| 9 | Task 6 | — | P2 |
| 10 | — | #3 | P1 |
| 11 | — | #4 | P1 |
| 12 | — | #5 | P1 |
| 13 | — | #7 | P1 |
| 14 | — | #14 | P2 |
| 15 | — | #13 | P2 |
| 16 | — | #15 | P2 |
| 17 | — | #18, #19 | P3 |
