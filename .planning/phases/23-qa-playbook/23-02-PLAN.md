---
phase: 23-qa-playbook
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - docs/QA-PLAYBOOK.md
autonomous: true
requirements:
  - QAPL-01

must_haves:
  truths:
    - "Skills and adaptive injection have documented test procedures"
    - "Memory flow has an end-to-end test procedure"
    - "Fallback chain has an end-to-end test procedure"
    - "Doctor and health checks each have test procedures"
    - "Observability (logging, session stats) has test procedures"
    - "Orchestrator pipeline has an abbreviated E2E test procedure"
    - "The playbook can be executed top-to-bottom to validate a release"
  artifacts:
    - path: "docs/QA-PLAYBOOK.md"
      provides: "Complete QA playbook covering all plugin features"
      contains: "## Orchestrator Pipeline"
  key_links:
    - from: "docs/QA-PLAYBOOK.md"
      to: "src/skills/adaptive-injector.ts"
      via: "Documents skill injection test procedures"
      pattern: "Adaptive Injection|skill.*detect"
    - from: "docs/QA-PLAYBOOK.md"
      to: "src/memory/"
      via: "Documents memory capture/retrieval test procedures"
      pattern: "Memory System|observation|retrieval"
---

<objective>
Complete the QA Playbook by adding sections for Skills and Adaptive Injection, Memory System, Fallback Chain, Doctor and Health Checks, Observability, and Orchestrator Pipeline E2E.

Purpose: Completes the playbook so the entire plugin can be validated in a single session (per D-01).
Output: Final `docs/QA-PLAYBOOK.md` covering all ~60 features across 9 sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-qa-playbook/23-CONTEXT.md
@.planning/phases/23-qa-playbook/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Skills, Memory, and Fallback sections</name>
  <files>docs/QA-PLAYBOOK.md</files>
  <read_first>
    - docs/QA-PLAYBOOK.md (existing content from Plan 01)
    - src/skills/adaptive-injector.ts (stack detection, filtering, token budgeting)
    - src/skills/loader.ts (skill loading from assets)
    - src/skills/dependency-resolver.ts (topological sort, cycle detection)
    - src/skills/linter.ts (asset validation)
    - assets/skills/typescript-patterns/SKILL.md (example skill with stacks/requires)
    - assets/skills/coding-standards/SKILL.md (universal skill example)
    - src/memory/capture.ts (event-driven observation capture)
    - src/memory/retrieval.ts (3-layer progressive disclosure)
    - src/memory/injector.ts (system prompt injection)
    - src/memory/decay.ts (relevance scoring)
    - src/memory/database.ts (SQLite schema, FTS5)
    - src/orchestrator/fallback/mock-interceptor.ts (mock test mode)
    - src/orchestrator/fallback/state-machine.ts (fallback state transitions)
    - src/orchestrator/fallback/error-classifier.ts (retryable error detection)
    - src/orchestrator/fallback/message-replay.ts (3-tier content degradation)
    - src/config.ts (fallback config options)
  </read_first>
  <action>
Append three sections to `docs/QA-PLAYBOOK.md`. Per D-09, each gets dedicated sections with end-to-end flow tests.

**Section 4: Skills and Adaptive Injection (per D-09)**
- `### Skill Loading` — Verify all 18 skills load from assets directory without errors
- `### Stack Detection` — Test auto-detection for TypeScript (package.json), Go (go.mod), Python (pyproject.toml/requirements.txt), Rust (Cargo.toml), Java (pom.xml), C# (*.csproj)
- `### Adaptive Filtering` — Verify only matching skills inject for a given stack (e.g., TypeScript project gets typescript-patterns + coding-standards but NOT go-patterns)
- `### Dependency Resolution` — Verify skills with `requires` fields load after their dependencies
- `### Token Budgeting` — Verify injection respects the 8000-token default budget
- `### Skill Linter` — Test asset validation catches malformed SKILL.md files
- **Negative tests:** Missing SKILL.md, circular dependency, invalid frontmatter

**Section 5: Memory System (per D-09)**
- `### Memory Database Setup` — Verify SQLite DB created at ~/.config/opencode/memory/
- `### Observation Capture` — Verify session events create observations in DB
- `### Retrieval and Ranking` — Verify relevance-scored retrieval returns top-N observations
- `### System Prompt Injection` — Verify memory context appears in system prompt
- `### Decay and Pruning` — Verify old observations decay in relevance score over time
- `### Cross-Project Isolation` — Verify project-level memories don't leak between projects
- **Negative tests:** Corrupted DB file, empty memory store

**Section 6: Fallback Chain (per D-09)**
- `### Mock Fallback Mode` — Enable via oc_mock_fallback, verify simulated failures trigger fallback
- `### Error Classification` — Verify rate-limit, timeout, quota, malformed errors are classified correctly
- `### State Machine Transitions` — Verify primary -> fallback -> exhausted transitions
- `### Message Replay` — Verify 3-tier content degradation (all parts -> text+images -> text only)
- `### Cooldown Recovery` — Verify model returns to primary after cooldown period
- **Negative tests:** All models exhausted, invalid fallback config
  </action>
  <verify>
    <automated>grep -c "^## \|^### " docs/QA-PLAYBOOK.md | xargs test 50 -le</automated>
  </verify>
  <acceptance_criteria>
    - "## Skills and Adaptive Injection" section exists
    - Contains subsections for Skill Loading, Stack Detection, Adaptive Filtering, Dependency Resolution, Token Budgeting, Skill Linter
    - "## Memory System" section exists
    - Contains subsections for Database Setup, Observation Capture, Retrieval, Injection, Decay
    - "## Fallback Chain" section exists
    - Contains subsections for Mock Mode, Error Classification, State Machine, Message Replay, Cooldown
    - Each subsection has Prerequisites, Steps, Expected Output, Pass/Fail
    - Each section has at least one negative test
  </acceptance_criteria>
  <done>Skills (18 skills, 6 test areas), Memory (5 test areas), and Fallback (5 test areas) sections complete with end-to-end flow tests and negative cases.</done>
</task>

<task type="auto">
  <name>Task 2: Add Doctor, Observability, and Orchestrator E2E sections</name>
  <files>docs/QA-PLAYBOOK.md</files>
  <read_first>
    - docs/QA-PLAYBOOK.md (existing content including Plan 01 and Task 1)
    - src/health/runner.ts (health check aggregation, reporting)
    - src/health/checks.ts (6 health checks — config, agent, asset, skill, memory, command)
    - src/observability/event-store.ts (in-memory event collection)
    - src/observability/event-handlers.ts (hook handlers for lifecycle events)
    - src/observability/context-monitor.ts (token tracking)
    - src/observability/log-writer.ts (JSON log persistence)
    - src/observability/retention.ts (30-day log pruning)
    - src/orchestrator/handlers/ (pipeline phase handlers — list directory to see all handlers)
    - src/tools/orchestrate.ts (orchestrateCore — entry point for E2E)
    - src/hooks/anti-slop.ts (anti-slop comment hook)
  </read_first>
  <action>
Append three final sections to `docs/QA-PLAYBOOK.md`, completing the playbook.

**Section 7: Doctor and Health Checks (per D-09)**
For each of the 6 health checks (config-validity, agent-injection, asset-directories, skill-loading, memory-db, command-accessibility):
- `### [check-name]`
- **What it checks:** Description from checks.ts
- **Steps:** How to trigger (via oc_doctor tool)
- **Expected Output:** Pass/warning/fail status with message
- **Negative Test:** Simulate failure condition (e.g., delete an asset directory, corrupt config)
- **Pass/Fail:** Clear criteria

Also include:
- `### Full Doctor Run` — Run oc_doctor and verify all 6 checks report status
- `### Anti-Slop Comment Hook` — Test that obvious/sycophantic comments are detected and blocked

**Section 8: Observability (per D-09)**
- `### Event Capture` — Verify tool invocations and lifecycle events are captured to event store
- `### Session Stats` — Invoke oc_session_stats and verify event counts, timeline
- `### Log Persistence` — Verify JSON logs written to ~/.config/opencode/logs/
- `### Log Retrieval` — Invoke oc_logs and verify filtered log output
- `### Log Retention` — Verify logs older than 30 days are pruned
- `### Context Monitor` — Verify token usage tracking reports context consumption
- **Negative tests:** Empty session (no events), corrupted log file

**Section 9: Orchestrator Pipeline E2E (per D-08)**
One abbreviated end-to-end procedure:
- `### Full Pipeline Run`
- **Prerequisites:** Clean state, no existing pipeline state file
- **Steps:** Invoke oc_orchestrate with a small, well-defined task (e.g., "Add a hello-world utility function")
- **Phases to Verify:** RECON produces research report, CHALLENGE proposes enhancements, ARCHITECT produces design, PLAN produces task list, BUILD creates commits, SHIP produces changelog, RETROSPECTIVE extracts lessons
- **Expected Output:** Pipeline completes all 8 phases, state file shows "completed"
- **Pass/Fail:** All phases reach "completed" status, artifacts exist for each phase

Also add a final section:
- `### Quick Smoke Test Checklist` — 10-item checklist for rapid validation (plugin loads, tools register, commands accessible, agents visible, skills inject, memory captures, doctor passes, logs write, config saves, stocktake counts match)

Update the Table of Contents at the top to include all 9 sections with correct anchor links.
  </action>
  <verify>
    <automated>grep -c "^## " docs/QA-PLAYBOOK.md | xargs test 9 -le</automated>
  </verify>
  <acceptance_criteria>
    - "## Doctor and Health Checks" section exists with 6 health check subsections plus Full Doctor Run and Anti-Slop
    - "## Observability" section exists with Event Capture, Session Stats, Log Persistence, Log Retrieval, Log Retention, Context Monitor
    - "## Orchestrator Pipeline" section exists with Full Pipeline Run subsection
    - Quick Smoke Test Checklist exists with at least 10 items
    - Table of Contents at top links to all 9 major sections
    - Total section count: 9 major sections (## level)
    - Total subsection count: at least 60 (### level) across entire document
    - Every subsection has Pass/Fail criteria
  </acceptance_criteria>
  <done>QA Playbook complete with all 9 sections. Every command (11), agent (8), tool (20), skill area, memory flow, fallback chain, health check, observability feature, and orchestrator pipeline has documented test procedures. Playbook is executable top-to-bottom for release validation per D-01.</done>
</task>

</tasks>

<verification>
- `wc -l docs/QA-PLAYBOOK.md` shows substantial document (expect 800+ lines)
- `grep -c "^## " docs/QA-PLAYBOOK.md` shows 9+ major sections
- `grep -c "^### " docs/QA-PLAYBOOK.md` shows 60+ subsections
- `grep -c "Pass/Fail" docs/QA-PLAYBOOK.md` shows 50+ occurrences
- `grep -c "Negative\|Error\|Invalid" docs/QA-PLAYBOOK.md` shows negative tests present
- `grep "Table of Contents\|Contents" docs/QA-PLAYBOOK.md` confirms TOC exists
- All 9 section headers present: Commands, Agents, Tools, Skills, Memory, Fallback, Doctor, Observability, Orchestrator
</verification>

<success_criteria>
- docs/QA-PLAYBOOK.md is a complete, self-contained QA playbook
- All 9 feature areas documented per D-02
- Every feature has step-by-step procedures per D-03
- Every feature has a negative test per D-04
- All 20 tools covered per D-05
- All 11 commands covered per D-06
- All 8 agents covered per D-07
- Orchestrator E2E covered per D-08
- Skills, memory, fallback, doctor covered per D-09
- Quick smoke test checklist enables rapid validation
</success_criteria>

<output>
After completion, create `.planning/phases/23-qa-playbook/23-02-SUMMARY.md`
</output>
