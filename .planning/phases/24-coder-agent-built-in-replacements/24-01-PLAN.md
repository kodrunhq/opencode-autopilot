---
phase: 24-coder-agent-built-in-replacements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/coder.ts
  - src/agents/index.ts
  - assets/commands/oc-tdd.md
  - tests/agents/coder.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Coder agent appears in Tab cycle between autopilot and debugger"
    - "Running /oc-tdd routes to the coder agent"
    - "Coder agent has tdd-workflow and coding-standards skills embedded in its prompt"
  artifacts:
    - path: "src/agents/coder.ts"
      provides: "Coder agent definition with embedded skills"
      exports: ["coderAgent"]
    - path: "assets/commands/oc-tdd.md"
      provides: "TDD command with agent: coder routing"
      contains: "agent: coder"
  key_links:
    - from: "src/agents/index.ts"
      to: "src/agents/coder.ts"
      via: "import and agents map entry"
      pattern: "coder: coderAgent"
    - from: "assets/commands/oc-tdd.md"
      to: "src/agents/coder.ts"
      via: "agent: coder frontmatter"
      pattern: "agent: coder"
---

<objective>
Create the primary Coder agent -- a pure implementer that writes code, runs tests, and fixes builds -- and route the /oc-tdd command to it.

Purpose: Users get a dedicated code-writing agent in their Tab cycle (per D-01 through D-06) that statically embeds TDD and coding-standards skills.
Output: `src/agents/coder.ts`, updated `src/agents/index.ts`, updated `assets/commands/oc-tdd.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-coder-agent-built-in-replacements/24-CONTEXT.md

<interfaces>
From src/agents/debugger.ts (reference pattern):
```typescript
import type { AgentConfig } from "@opencode-ai/sdk";

export const debuggerAgent: Readonly<AgentConfig> = Object.freeze({
  description: "...",
  mode: "all",
  maxSteps: 25,
  prompt: `...`,
  permission: {
    edit: "allow",
    bash: "allow",
    webfetch: "deny",
  } as const,
});
```

From src/agents/index.ts (agents map -- alphabetical order = Tab cycle order):
```typescript
export const agents = {
  autopilot: autopilotAgent,
  // coder goes HERE (between autopilot and debugger)
  debugger: debuggerAgent,
  documenter: documenterAgent,
  metaprompter: metaprompterAgent,
  planner: plannerAgent,
  "pr-reviewer": prReviewerAgent,
  researcher: researcherAgent,
  reviewer: reviewerAgent,
} as const;
```

Current oc-tdd.md frontmatter has `description` but no `agent` field. Add `agent: coder`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Coder agent with embedded TDD and coding-standards skills</name>
  <files>src/agents/coder.ts, src/agents/index.ts, tests/agents/coder.test.ts</files>
  <read_first>
    - src/agents/debugger.ts (reference primary agent pattern with embedded skill)
    - src/agents/planner.ts (another reference with two embedded skills)
    - src/agents/index.ts (agents map, import pattern, configHook)
    - assets/skills/tdd-workflow/SKILL.md (skill content to embed -- strip YAML frontmatter)
    - assets/skills/coding-standards/SKILL.md (skill content to embed -- strip YAML frontmatter)
  </read_first>
  <behavior>
    - Test 1: coderAgent.mode equals "all" (D-05)
    - Test 2: coderAgent.description is a non-empty string
    - Test 3: coderAgent.prompt contains skill name="tdd-workflow" tag (D-03)
    - Test 4: coderAgent.prompt contains skill name="coding-standards" tag (D-03)
    - Test 5: coderAgent.permission.edit equals "allow" (D-04)
    - Test 6: coderAgent.permission.bash equals "allow" (D-04)
    - Test 7: coderAgent.permission.webfetch is undefined or "deny" (D-04 -- no web access)
    - Test 8: coderAgent is frozen (Object.isFrozen returns true)
    - Test 9: agents map has "coder" key (from index.ts)
    - Test 10: agents map keys are in alphabetical order (Tab cycle ordering)
  </behavior>
  <action>
    Create `src/agents/coder.ts` following the exact pattern from `src/agents/debugger.ts`:

    1. Import `AgentConfig` from `@opencode-ai/sdk`
    2. Export `coderAgent: Readonly<AgentConfig>` wrapped in `Object.freeze()`
    3. Set fields per locked decisions:
       - `description`: "Pure code implementer: writes production code, runs tests, fixes builds -- with TDD workflow and coding standards"
       - `mode: "all"` (D-05 -- Tab cycle + @ autocomplete)
       - `maxSteps: 30` (between debugger's 25 and autopilot's 50)
       - `permission`: `{ edit: "allow", bash: "allow", webfetch: "deny" } as const` (D-04)
    4. Build the `prompt` string:
       - Role intro: "You are the coder agent. You are a pure code implementer. You write production code, run tests, and fix builds. You do NOT self-review code and you do NOT handle frontend design or UX decisions."
       - Explain how the agent works (write code, run tests, iterate until green, commit)
       - Embed `tdd-workflow` skill content: read `assets/skills/tdd-workflow/SKILL.md`, strip everything before the first line starting with `#` (removes YAML frontmatter), wrap in `<skill name="tdd-workflow">...</skill>`
       - Embed `coding-standards` skill content: same process with `assets/skills/coding-standards/SKILL.md`, wrap in `<skill name="coding-standards">...</skill>`
       - Add Rules section: "ALWAYS follow TDD workflow", "NEVER self-review", "NEVER make UX/design decisions", "Use bash to run tests after every code change", "Commit with descriptive messages"

    5. Update `src/agents/index.ts`:
       - Add `import { coderAgent } from "./coder";` with other imports
       - Add `coder: coderAgent,` to the `agents` map between `autopilot` and `debugger` (alphabetical order ensures Tab cycle: autopilot, coder, debugger, planner, reviewer per D-01)
       - Add `export { coderAgent } from "./coder";` with other re-exports

    6. Create `tests/agents/coder.test.ts` with tests for all behaviors listed above.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-autopilot && bun test tests/agents/coder.test.ts</automated>
  </verify>
  <done>
    - src/agents/coder.ts exports coderAgent with mode "all", maxSteps 30, embedded tdd-workflow and coding-standards skills, permission edit/bash allow, webfetch deny
    - src/agents/index.ts agents map contains "coder" key in alphabetical position between "autopilot" and "debugger"
    - All tests in tests/agents/coder.test.ts pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Route /oc-tdd command to coder agent</name>
  <files>assets/commands/oc-tdd.md</files>
  <read_first>
    - assets/commands/oc-tdd.md (current content -- needs agent: coder added to frontmatter)
  </read_first>
  <action>
    Update `assets/commands/oc-tdd.md` YAML frontmatter to add `agent: coder` (per D-06). The frontmatter should have both `description` and `agent: coder` fields. Keep the body text (TDD instructions, language detection, $ARGUMENTS) completely unchanged.
  </action>
  <verify>
    <automated>grep -q "agent: coder" /home/joseibanez/develop/projects/opencode-autopilot/assets/commands/oc-tdd.md && echo "PASS: agent routing present" || echo "FAIL: missing agent routing"</automated>
  </verify>
  <acceptance_criteria>
    - assets/commands/oc-tdd.md contains `agent: coder` in YAML frontmatter
    - assets/commands/oc-tdd.md still contains `description: Implement a feature using strict RED-GREEN-REFACTOR TDD methodology`
    - Body text ($ARGUMENTS, language detection instructions) unchanged
  </acceptance_criteria>
  <done>
    - /oc-tdd command routes to the coder agent via agent: coder frontmatter
  </done>
</task>

</tasks>

<verification>
- `bun test tests/agents/coder.test.ts` passes
- `grep "coder: coderAgent" src/agents/index.ts` returns a match
- `grep "agent: coder" assets/commands/oc-tdd.md` returns a match
- `bun run lint` passes
</verification>

<success_criteria>
1. Coder agent exists at src/agents/coder.ts with mode "all", embedded tdd-workflow and coding-standards skills
2. Agents map in index.ts has coder in alphabetical position (autopilot, coder, debugger, ...)
3. /oc-tdd command has agent: coder frontmatter
4. All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/24-coder-agent-built-in-replacements/24-01-SUMMARY.md`
</output>
