---
phase: 24-coder-agent-built-in-replacements
plan: 04
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/agents/coder.ts
  - src/agents/autopilot.ts
  - src/agents/debugger.ts
  - src/agents/pipeline/oc-implementer.ts
  - src/agents/index.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "All code-writing agents mention oc_hashline_edit in their prompts"
    - "Built-in Plan agent is suppressed via config hook"
    - "All agents registered, all commands routed, all skills loading"
  artifacts:
    - path: "src/agents/coder.ts"
      provides: "Coder agent prompt updated with hashline-edit guidance"
      contains: "oc_hashline_edit"
    - path: "src/agents/autopilot.ts"
      provides: "Autopilot prompt updated with hashline-edit guidance"
      contains: "oc_hashline_edit"
    - path: "src/agents/debugger.ts"
      provides: "Debugger prompt updated with hashline-edit guidance"
      contains: "oc_hashline_edit"
    - path: "src/agents/pipeline/oc-implementer.ts"
      provides: "oc-implementer prompt updated with hashline-edit guidance"
      contains: "oc_hashline_edit"
  key_links:
    - from: "src/agents/index.ts"
      to: "config.agent"
      via: "configHook sets disable: true on built-in Plan"
      pattern: "disable.*true"
---

<objective>
Update all code-writing agent prompts to prefer oc_hashline_edit, suppress the built-in Plan agent, and run a wiring audit to verify the entire agent/command/skill ecosystem is connected.

Purpose: Completes the phase by wiring the hashline-edit tool into agent prompts (D-12), suppressing the redundant built-in Plan agent (D-17/D-18), and verifying everything works together (D-19/D-20).
Output: Updated agent prompts, Plan suppression in configHook, verified wiring
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-coder-agent-built-in-replacements/24-CONTEXT.md
@.planning/phases/24-coder-agent-built-in-replacements/24-01-SUMMARY.md
@.planning/phases/24-coder-agent-built-in-replacements/24-02-SUMMARY.md

<interfaces>
<!-- Key types from prior plans and codebase -->

From src/agents/index.ts (configHook — where Plan suppression goes):
```typescript
export async function configHook(config: Config, configPath?: string): Promise<void> {
  if (!config.agent) {
    config.agent = {};
  }
  // ... load plugin config, resolve groups/overrides ...
  registerAgents(agents, config, groups, overrides);
  registerAgents(pipelineAgents, config, groups, overrides);
  // ADD: Plan suppression logic HERE
}
```

AgentConfig.disable field (from @opencode-ai/sdk):
```typescript
export type AgentConfig = {
  disable?: boolean;
  mode?: "subagent" | "primary" | "all";
  // ...
};
```

Hashline-edit prompt guidance pattern (from RESEARCH.md):
```
## Editing Files

When editing files, prefer oc_hashline_edit over the built-in edit tool.
Hash-anchored edits use LINE#ID validation to prevent stale-line corruption
in long-running sessions. The built-in edit is still available as a fallback.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hashline-edit guidance to all code-writing agent prompts</name>
  <files>src/agents/coder.ts, src/agents/autopilot.ts, src/agents/debugger.ts, src/agents/pipeline/oc-implementer.ts</files>
  <read_first>
    - src/agents/coder.ts (prompt to update — created in 24-01)
    - src/agents/autopilot.ts (full file — find prompt string to append to)
    - src/agents/debugger.ts (full file — find prompt string to append to)
    - src/agents/pipeline/oc-implementer.ts (full file — find prompt string to append to)
  </read_first>
  <action>
    Add the following section to the `prompt` string of each of these four agents (per D-12). Append it near the end of the prompt, before the final Rules section if one exists, or at the end:

    ```
    ## Editing Files

    When editing files, prefer oc_hashline_edit over the built-in edit tool. Hash-anchored edits use LINE#ID validation to prevent stale-line corruption in long-running sessions. Each edit targets a line by its number and a 2-character content hash (e.g., 42#VK). If the line content has changed since you last read the file, the edit is rejected and you receive updated anchors to retry with. The built-in edit tool is still available as a fallback.
    ```

    **For `src/agents/coder.ts`:**
    Insert before the `## Rules` section in the prompt.

    **For `src/agents/autopilot.ts`:**
    Insert before the `## Rules` section in the prompt.

    **For `src/agents/debugger.ts`:**
    Insert before the `## Rules` section in the prompt.

    **For `src/agents/pipeline/oc-implementer.ts`:**
    Insert before the `## Rules` section (step 6 onward talks about writing code, this fits before it) or at the end of the prompt.
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-autopilot && grep -l "oc_hashline_edit" src/agents/coder.ts src/agents/autopilot.ts src/agents/debugger.ts src/agents/pipeline/oc-implementer.ts | wc -l | grep -q "4" && echo "PASS: all 4 agents updated" || echo "FAIL: not all agents updated"</automated>
  </verify>
  <acceptance_criteria>
    - `src/agents/coder.ts` prompt contains "oc_hashline_edit"
    - `src/agents/autopilot.ts` prompt contains "oc_hashline_edit"
    - `src/agents/debugger.ts` prompt contains "oc_hashline_edit"
    - `src/agents/pipeline/oc-implementer.ts` prompt contains "oc_hashline_edit"
    - Each prompt contains the phrase "prefer oc_hashline_edit over the built-in edit tool"
    - Each prompt still contains its original content (skills, rules, etc.)
  </acceptance_criteria>
  <done>
    - All 4 code-writing agents have hashline-edit preference guidance in their prompts
  </done>
</task>

<task type="auto">
  <name>Task 2: Suppress built-in Plan agent via config hook</name>
  <files>src/agents/index.ts</files>
  <read_first>
    - src/agents/index.ts (full file — configHook function where suppression goes)
  </read_first>
  <action>
    Add built-in Plan agent suppression to the `configHook` function in `src/agents/index.ts` (per D-17, D-18).

    After the two `registerAgents()` calls and before the function ends, add:

    ```typescript
    // Suppress built-in Plan agent — our planner agent replaces it (D-17).
    // Try common naming variants since the exact built-in name is undocumented.
    const planVariants = ["Plan", "plan", "Planner", "planner"] as const;
    for (const variant of planVariants) {
      if (config.agent[variant] !== undefined) {
        config.agent[variant] = {
          ...config.agent[variant],
          disable: true,
        };
      }
    }
    ```

    This is safe because:
    - If no built-in Plan agent exists, the loop does nothing
    - If a user has custom-defined an agent named "Plan", the disable flag prevents Tab clutter (our planner is the intended replacement)
    - The `disable: true` field is on the `AgentConfig` type from `@opencode-ai/sdk`
  </action>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-autopilot && grep -q "disable.*true" src/agents/index.ts && grep -q "planVariants" src/agents/index.ts && echo "PASS: Plan suppression added" || echo "FAIL: Plan suppression missing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/agents/index.ts` configHook contains Plan agent suppression logic
    - Suppression checks multiple name variants: "Plan", "plan", "Planner", "planner"
    - Suppression sets `disable: true` on the matching config entry
    - Suppression runs after `registerAgents()` calls
    - `bunx tsc --noEmit` passes (disable field is valid on AgentConfig type)
  </acceptance_criteria>
  <done>
    - Built-in Plan agent is suppressed via disable: true in config hook
    - Suppression is safe (no-op if Plan doesn't exist)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wiring audit — verify all agents, commands, skills connected</name>
  <files>src/agents/index.ts, src/index.ts, assets/commands/oc-tdd.md</files>
  <action>Run all verification commands listed in how-to-verify section and report results to user for approval.</action>
  <what-built>
    Phase 24 complete: Coder agent created, oc_hashline_edit tool built, wave auto-assignment wired, agent prompts updated, built-in Plan suppressed.
  </what-built>
  <how-to-verify>
    Run these commands to verify the full wiring:

    1. Type check: `bunx tsc --noEmit` — should pass with zero errors
    2. All tests: `bun test` — all tests should pass
    3. Lint: `bun run lint` — should be clean
    4. Agent count in index.ts: `grep -c "Agent" src/agents/index.ts` — verify agents map has coder entry
    5. Command routing: `grep "agent:" assets/commands/oc-tdd.md` — should show `agent: coder`
    6. Hashline tool registered: `grep "ocHashlineEdit" src/index.ts` — should show import + tools array
    7. Wave assigner wired: `grep "assignWaves" src/orchestrator/handlers/build.ts` — should show import + call
    8. Plan suppression: `grep "disable" src/agents/index.ts` — should show Plan suppression logic
    9. All 4 agents have hashline guidance: `grep -l "oc_hashline_edit" src/agents/coder.ts src/agents/autopilot.ts src/agents/debugger.ts src/agents/pipeline/oc-implementer.ts` — should list all 4 files

    If any check fails, describe the issue and it will be fixed.
  </how-to-verify>
  <verify>
    <automated>cd /home/joseibanez/develop/projects/opencode-autopilot && bunx tsc --noEmit && bun test && bun run lint</automated>
  </verify>
  <done>All verification commands pass and user approves the wiring audit</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` passes
- `bun test` all tests pass
- `bun run lint` clean
- All 4 code-writing agents contain "oc_hashline_edit" in their prompts
- configHook contains Plan suppression logic
- `/oc-tdd` routes to coder agent
- oc_hashline_edit registered in plugin tools
- wave-assigner wired into build handler
</verification>

<success_criteria>
1. All 4 code-writing agents prefer oc_hashline_edit in their prompts
2. Built-in Plan agent suppressed via disable: true
3. Full wiring verified: types pass, tests pass, lint clean
4. Human confirms the full phase is wired correctly
</success_criteria>

<output>
After completion, create `.planning/phases/24-coder-agent-built-in-replacements/24-04-SUMMARY.md`
</output>
